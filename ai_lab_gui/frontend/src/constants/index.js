/**
 * CONSTANTS INDEX — PHASE 1 FOUNDATION
 * 
 * Barrel export for all workflow constants.
 */

export {
  STATUS_COLOR,
  RISK_COLOR,
  WORKFLOW_LIFECYCLE,
  STEP_LIFECYCLE,
  PROJECTION_STATE,
  STATUS_LABEL,
  PROJECTION_STATE_LABEL,
  formatRetryCount,
  formatStepNumber,
  getStatusClassSuffix,
  isTerminalStatus,
  isActiveStatus,
} from "./workflow.js";
