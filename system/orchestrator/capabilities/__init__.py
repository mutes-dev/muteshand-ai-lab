"""Capabilities package — bounded domain normalizers/compilers.

Per AGENT_CAPABILITY_ROUTING_CONTRACT_V1:
- Capabilities emit candidate workflow/DAG structure only
- Capabilities do NOT execute tools, own lifecycle, or bypass compiler/validator
"""
