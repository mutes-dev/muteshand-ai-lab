# AI LAB — MASTER IMPLEMENTATION CHECKLIST

**Purpose:** The single detailed checklist for building, auditing, and verifying the entire system. If every box is checked, the system is complete and aligned.

**How to use this document:**
- Build in phase order
- Check boxes only when verified by evidence (test, harness, code review)
- Use unchecked boxes to detect drift
- Do not skip phases

**Architecture Authority:** ROADMAP_V2_TEMP.txt, DOCUMENT INTERPRETATION MODEL.txt
**Contracts:** Individual CONTRACT_V1.txt files per DIM
**Implementation Roadmap:** IMPLEMENTATION_ROADMAP_V2.txt
**Development Roadmap:** DEVELOPMENT_ROADMAP_V2.txt

**Last Updated:** 2026-06-02 (Sprint 4 ISSUE-068A risk/effort audit complete; ISSUE-070 passed with observations; ISSUE-071 pending closure validation)

---

## PHASE 1 — CORE SYSTEM & EXECUTION FOUNDATION

**Goal:** The system has a single, deterministic entrypoint. Steps are well-defined. State transitions are predictable.

**Status:** STABILIZED — Core pipeline locked, authority boundaries verified, architecture coherent

**Reference Documents:**
- SYSTEM_CONTRACTS_V2.txt
- STEP_SCHEMA_CONTRACT_V1.txt
- STEP_IO_CONTRACT_V1.txt
- STEP_RESOLUTION_CONTRACT_V1.txt
- INTERFACE_CONTRACT_V1.txt
- STATE_TRANSITIONS_CONTRACT_V1.txt

**Checklist:**

- [x] Single `system_entry` exists and is enforced (`system/entry/system_entry.py`)
- [x] No other entrypoints can trigger execution (`core/entry.py` is utility-only, not duplicate)
- [x] `system_entry` is deterministic (same input → same execution path)
- [x] Step schema is defined and validated (enforced throughout pipeline)
- [~] Step I/O contracts are enforced (inputs present, outputs valid) — partially enforced
- [x] Step resolution is deterministic (`system/resolver/argument_resolver.py`, `core/argument_resolver.py`)
- [x] State transitions are defined for all lifecycle states (`workflow_control.py` FSM)
- [~] Illegal state transitions are rejected — FSM exists but `_internal=True` bypasses validation
- [x] Interface contracts are enforced (external API boundaries) — `system_entry.py` enforces production flag
- [x] Core execution pipeline is deterministic and traceable

**Done When:**
- Execution can be triggered only through `system_entry`
- Step schema validation passes
- State transitions are deterministic and legal

**Drift Detection:**
- Multiple entrypoints exist
- Steps bypass schema validation
- State transitions are inconsistent

---

## PHASE 2 — LIFECYCLE AUTHORITY

**Goal:** Exactly one layer owns lifecycle truth. Transitions are deterministic. No authority fragmentation.

**Status:** ALIGNED — Implementation correctly binds lifecycle authority to runtime registry per LIFECYCLE_AND_PROJECTION_AUTHORITY_CONTRACT_V1 §SINGLE LIFECYCLE AUTHORITY RULE. `_workflow_state_registry` is the current concrete implementation of the Lifecycle Authority architectural role.

**Reference Documents:**
- LIFECYCLE_AUTHORITY_CONTRACT_V1.txt
- LIFECYCLE_AND_PROJECTION_AUTHORITY_CONTRACT_V1.txt

**Checklist:**

- [x] Exactly ONE layer owns lifecycle truth — `_workflow_state_registry` correctly implements sole lifecycle authority per contract
- [x] No other layer mirrors, caches, or duplicates lifecycle state as authority — persistence correctly injects authoritative lifecycle before exposing (inject_authoritative_lifecycle_into_workflow)
- [x] Lifecycle transitions are deterministic (no ambiguous paths) — FSM defined in `workflow_control.py`
- [~] Lifecycle state changes are atomic where required — `threading.RLock` used; atomicity partially verified
- [x] No projection layer synthesizes lifecycle truth — ProjectionManager correctly derives from runtime authority, emits downward-only, does NOT synthesize
- [x] No stream/registry overwrites lifecycle state — Stream registry correctly caches projection state, consumes from authority via _get_workflow_state()
- [~] Lifecycle authority is independent from synchronization success — partially; persistence failure is non-fatal
- [~] Lifecycle commits before downstream synchronization — partially implemented
- [x] Authority fragmentation is impossible (no split ownership) — `core/` provides deterministic utilities; `system/` owns execution authority. No split lifecycle ownership.

**Done When:**
- Singular lifecycle authority confirmed
- No layer can override lifecycle state
- Projections consume but never produce lifecycle truth

**Drift Detection:**
- Multiple components claim lifecycle ownership
- Projections update lifecycle state
- Registry/stream caches treated as lifecycle truth

---

## PHASE 3 — EXECUTION STRUCTURE

**Goal:** Dependencies form a valid DAG. Execution groups are correct. Conflicts are detected.

**Status:** COMPLETE + STABILIZED — scheduling, conflict detection, dependency validation, and execution sequencing verified

**Reference Documents:**
- DEPENDENCY_MODEL_CONTRACT_V1.txt
- EXECUTION_SCHEDULING_CONTRACT_V1.txt
- CONFLICT_RESOLUTION_CONTRACT_V1.txt
- PLAN_STEP_CONTRACT_V1.txt

**Checklist:**

- [~] Dependencies form a DAG (no cycles) — `workflow_validator.py` validates; needs adversarial verification
- [x] `depends_on` semantics are enforced — `execution_scheduler.py` §3 enforces dependency completion
- [x] Execution groups are derived correctly from dependencies — `execution_scheduler.py` `create_execution_group()`
- [x] Parallel eligibility is determined safely — `_check_parallel_eligibility()` with destructive-type exclusion
- [x] Resource conflicts are detected and resolved — `ConflictDetector` class exists; severity rules implemented
- [x] Step definition structure is enforced — `workflow_validator.py` enforces structure
- [x] Plan validation rejects invalid structures — `workflow_validator.py` fail-fast validation
- [x] Dependency invalidation propagates correctly — `_invalidate_dependents()` recursively invalidates downstream steps; `execution_scheduler.py` re-evaluates dependencies using live `steps_map` (authoritative) over stale `step_states` (snapshot fallback)
- [x] Stale dependencies are rejected — `_check_dependencies_satisfied()` prioritizes live `steps_map` status; FAILED dependencies block dependent steps; stale BLOCKED/PENDING snapshots do not survive recomputation

**Done When:**
- All workflows have valid DAG structures
- Execution ordering is deterministic
- Conflicts are resolved without deadlock

**Drift Detection:**
- Circular dependencies allowed
- Execution order is non-deterministic
- Conflicts cause crashes or silent corruption

---

## PHASE 4 — ORCHESTRATION & RUNTIME COORDINATION

**Goal:** Orchestrator sequences execution correctly. Runtime registry tracks active work. No authority leakage.

**Status:** STABILIZED — orchestration survivability operationally verified; runtime lifecycle continuity validated under pause/resume/mutation/replay; remaining work focused on semantic refinement and recoverability derivation

**Reference Documents:**
- ORCHESTRATION_AND_EXECUTION_SEQUENCE_CONTRACT_V1.txt
- RUNTIME_REGISTRY_AND_EXECUTION_COORDINATION_CONTRACT_V1.txt
- ORCHESTRATOR_CONTRACT_V2.txt
- EXECUTION_RUNTIME_GOVERNANCE_CONTRACT_V1.txt

**Checklist:**

- [x] Orchestration sequencing is deterministic — `orchestrator_runtime.py` loop is deterministic
- [x] Execution choreography follows defined ordering — `execution_scheduler.py` sequences correctly
- [x] Terminalization sequencing is correct — `finalize_workflow_from_execution()` implements terminal convergence; `stop_workflow()` implements authoritative terminal convergence with projection invalidation + persistence + cleanup
- [x] Pause/resume preserves state without replay — PAUSED state validated; `pause_workflow` rejects non-ACTIVE; `resume_workflow` hard-guards persistence + FSM validation + ACTIVE promotion; warm restoration + quarantine eliminate cold-start gaps
- [x] Retry sequencing is legal — `retry_step()` in `workflow_control.py` validates FAILED/BLOCKED, increments execution_generation, clears artifacts, invalidates dependents, emits projection; governance-authorized only
- [x] Runtime registry owns active execution coordination — `_workflow_state_registry` in `workflow_control.py`
- [x] Runtime registry owns worker coordination — `execution_scheduler.py` tracks active steps
- [x] Runtime registry owns runtime locks — `threading.RLock` used in `_workflow_state_lock`, `ConflictDetector._lock`, `trace_collector._collectors_lock`
- [x] Runtime registry tracks orphan executions — `execution_generation` counter increments on retry; stale owner suppression active; `warm_registry_from_disk` + quarantine eliminate orphan resurrection
- [x] Runtime registry DOES own lifecycle authority — Correct per LIFECYCLE_AND_PROJECTION_AUTHORITY_CONTRACT_V1: "Runtime registry is sole authority for ALL lifecycle state"
- [x] Runtime registry does NOT own projection authority — projections managed by `projection_manager.py`
- [x] Runtime registry does NOT own persistence authority — persistence in separate `persistence.py`
- [x] Orchestrator coordinates but does NOT own lifecycle truth — orchestrator calls `request_step_transition()` and `_update_workflow_state()` — the only functions that mutate lifecycle state
- [x] Governance participates in choreography but does not own sequencing — `governance.py` makes decisions, scheduler owns sequencing
- [x] Orphan cleanup is deterministic — `_rollback_partial_state()` exists; `stop_workflow()` cleans checkpoints + projection stores on terminal; S9E periodic orphan cleanup active
- [x] ACTIVE continuity restoration validated across refresh/reconnect boundaries
- [x] Reattachment continuity operationally validated
- [x] PENDING_RECOVERY convergence normalization operationally validated
- [x] Workflow resurrection suppression validated during reconnect hydration
- [x] Runtime authority continuity preserved during frontend detachment/reconnect
- [x] frontend-detach recovery continuity operationally validated
- [x] Task Hub recovery survivability operationally validated
- [x] operator-driven workflow reattachment continuity operationally validated
- [x] post-recovery pause/resume continuity operationally validated
- [x] post-recovery execution continuation operationally validated
- [x] FAILED workflow terminal convergence operationally verified — failed workflows converge to operator-visible FAILED terminal state without stale RUNNING/ACTIVE surfacing
- [x] Causative failed-step preservation verified — divide-by-zero causative step remains FAILED rather than stale BLOCKED
- [x] Downstream dependency-victim blocking verified — downstream dependent steps remain BLOCKED after causative failure
- [x] Authoritative retry target metadata operationalized — backend-authored retry_target_step_id exposed and frontend-consumed
- [x] Failed workflow refresh reattachment verified — focused FAILED workflow terminal projection rehydrates after refresh without resuming execution

Clarification: These are lifecycle surface / projection continuity verifications only. Runtime registry remains lifecycle authority. Projection and frontend remain non-authoritative.

Additional replay-driven operational clarification (2026-05-27):

* refresh continuity and detach/recovery continuity are separate operational semantics
* runtime authority continuity can remain correct while foreground attachment continuity changes
* Task Hub recovery semantics are operationally distinct from simple hydration continuity
* replay-driven operational validation confirmed backend execution independence from frontend attachment continuity
* backend pre-registration continuity stabilization operationally implemented
* planning-phase workflow identity continuity operationally stabilized
* refresh during pre-planning execution window now preserves authoritative workflow attachment continuity
* frontend attachment continuity now anchors to pre-registered authoritative workflow identity rather than delayed planner materialization
* replay-driven validation confirmed backend identity timing gaps were the true root cause of planning refresh continuity instability

**Done When:
- Orchestrator sequences without claiming lifecycle authority
- Runtime registry coordinates without claiming authority
- Workers are tracked; orphans are cleaned
- Pause/resume is safe

**Drift Detection:**
- Orchestrator mutates lifecycle state directly
- Runtime registry claims terminalization truth
- Orphan executions persist indefinitely

---

## PHASE 5 — EXECUTION IDENTITY & REPLAY

**Goal:** Every execution has a clear identity. Replay is explicit. Retry is isolated.

**Status:** MOSTLY VERIFIED — execution lineage continuity, retry-generation visibility, execution-generation visibility, durable replay continuity, and replay cursor stabilization operationalized; replay engine intentionally absent

**Reference Documents:**
- EXECUTION_IDENTITY_AND_REPLAY_CONTRACT_V1.txt

**Checklist:**

- [x] Every execution has a unique identity — `execution_generation` operationalized and validated through replay continuity and stale-owner suppression runtime verification
- [x] Retry generates a NEW execution identity — retry increments execution_generation and preserves retry lineage isolation; runtime validated
- [x] Retry is NOT a continuation of terminated execution — retry lineage isolation operationally verified; stale-owner suppression validated
- [x] Replay is explicit-only (never automatic) — no automatic replay mechanism found
- [x] Replay does NOT resurrect terminated execution — no replay-resurrection found
- [x] Reconstruction restores state WITHOUT creating new lineage — persistence restore normalizes to PENDING_RECOVERY; replay continuity preserves lineage semantics
- [x] Reconstruction and replay are semantically distinct — reconstruction normalizes lifecycle state; replay continuity preserves chronology only
- [x] Stale executions are invalidated — execution_generation stale-owner suppression operationally validated through replay continuity testing
- [x] Execution lineage is traceable — `traces/` directory has 200+ workflow traces
- [x] Replay observability exists — execution-generation visibility, retry-generation visibility, transition chronology visibility, and replay continuity visibility operationalized
- [x] Durable replay chronology continuity operationalized — append-only chronology journals and continuity-aware hydration verified
- [x] Replay continuity survives reconnect/restart — restart-safe chronology restoration operationalized
- [x] Replay cursor semantics are stable — bus_sequence_id operationalized as authoritative chronology cursor
- [x] Replay continuity remains transport-agnostic — chronology continuity semantics verified independent from polling transport

**Done When:**
- Retry always creates new identity
- Replay requires explicit action
- Reconstruction preserves semantics without replay

**Drift Detection:**
- Retry reuses old execution ID
- Automatic replay during recovery
- Reconstruction treated as replay

---

## PHASE 6 — MUTATION LEGALITY & RUNTIME ISOLATION

**Goal:** No illegal mutation is possible. Active steps are immutable. Concurrent mutations are rejected.

**Status:** VERIFIED — mutation legality enforcement, operational governance visibility, cancellation legality, retry lineage isolation, and convergence continuity operationally validated

**Reference Documents:**
- MUTATION_LEGALITY_AND_RUNTIME_ISOLATION_CONTRACT_V1.txt
- CONFLICT_RESOLUTION_CONTRACT_V1.txt

**Checklist:**

- [x] ACTIVE steps are mutable with restart semantics — `mutation_validation.py` `validate_step_mutable` allows ACTIVE edits with restart; `edit_step` resets ACTIVE→PENDING with artifact invalidation
- [x] Retry window is protected by execution_generation — `execution_generation` increments on retry; stale owner suppression active in execution loop
- [x] Same-workflow mutations route through `plan_mutation_manager` — `request_plan_mutation` is single entry point; validation + trace + projection re-emission implemented
- [x] Cross-workflow mutation safety enforced — `plan_mutation_manager` operates per-workflow; no cross-workflow mutation path exists
- [x] Dependency invalidation propagates correctly — `_invalidate_dependents()` recursively clears execution_result/output/blocked_reason for downstream steps; preserves FAILED terminality
- [x] Stale execution IDs are rejected — `execution_generation` validation in execution loop suppresses stale owners
- [x] Runtime locking prevents illegal concurrent access — `threading.RLock` on `_workflow_state_lock`, conflict_detector, trace_collector
- [x] Mutation legality visibility exists in control layers — `validate_workflow_mutable` and `validate_step_mutable` enforce legality; frontend `PlanMutationPanel` hidden for terminal workflows
- [x] Retry isolation is enforced — `retry_step` increments execution_generation; stale execution owners suppressed; checkpoint deleted; dependents invalidated
- [x] Invalidation chains terminate correctly — `_invalidate_dependents` stops at terminal steps (FAILED not reset); no infinite recursion observed
- [x] ACTIVE mutation legality visibility operationalized — governance-aware mutation rejection UX operationalized
- [x] Cancellation legality operationalized — CANCELLED workflows immutable and non-retryable
- [x] Convergence-state visibility operationalized — projection convergence continuity visible during mutation/retry operations
- [x] WorkflowStudio operational governance remains non-authoritative — operational mutations remain legality-constrained and projection-derived
- [x] ISSUE-065 operational mutation validation completed
- [x] ACTIVE mutation rejection validated
- [x] PAUSED operational metadata edit validated
- [x] metadata refresh survival validated
- [x] metadata detach/reattach survival validated
- [x] metadata app close/reopen survival validated
- [x] resume after metadata edit validated
- [x] expected_outcome confirmed non-authoritative
- [x] terminal COMPLETED mutation guard validated

**Clarification (2026-05-17):**
Semantic mutation invalidates derived execution artifacts (tool_call, execution_result, output, blocked_reason, validator artifacts) — verified in `edit_step`, `_handle_edit_step`, and `retry_step`. Scheduler snapshots (`step_states`) are cache/fallback only; live `steps_map` holds authoritative status. Semantic invalidation propagation and semantic recomputation remain PARTIAL (observational scaffolding only, not execution authority).

Additional Clarification (2026-05-18):
Semantic invalidation lifecycle classification reconciled:

* semantic_expectation remains advisory-only
* drift signals remain observational only
* semantic invalidation does NOT participate in lifecycle authority
* semantic regeneration is deterministic but non-authoritative
* semantic recomputation gaps affect observability only, not execution safety

ACTIVE execution ownership is immutable.

Mutation requests during ACTIVE execution do NOT directly mutate executing runtime state.

Instead:
- execution_generation invalidation occurs
- controlled restart/re-entry semantics occur
- stale execution owners are invalidated
- legality windows remain enforced

**Done When:**
- ACTIVE mutation is impossible
- Concurrent mutation is rejected
- Invalidation propagates deterministically

**Drift Detection:**
- ACTIVE step mutated
- Concurrent mutations succeed
- Invalidation does not propagate

---

## PHASE 7 — PERSISTENCE, RECOVERY & CONVERGENCE

**Goal:** System survives restart. Recovery restores authority. Convergence is downward-only. Stale state is quarantined.

**Status:** VERIFIED — downward-only convergence, durable replay continuity, restart-safe chronology restoration, and continuity-aware hydration operationally validated

**Reference Documents:**
- SYSTEM_CONVERGENCE_AND_RECOVERY_CONTRACT_V1.txt
- PERSISTENCE_AND_DURABILITY_CONTRACT_V1.txt
- MEMORY_STORAGE_CONTRACT_V1.txt

**Checklist:**

- [~] Persistence mirrors authority (does not claim it) — `persistence.py` writes workflow state; claims to be "compatibility mirror"
- [x] Recovery restores runtime state from persistence — `orchestrator_runtime.py` restores step statuses from persisted state
- [x] Recovery does NOT replay execution — persistence restore normalizes ACTIVE→PENDING_RECOVERY; re-entry requires explicit `run_workflow` call; no automatic replay
- [x] Recovery converges to authoritative lifecycle state — `warm_registry_from_disk()` populates registry from disk then normalizes; `inject_authoritative_lifecycle_into_workflow()` overwrites with registry truth
- [x] Convergence is downward-only (authority → derived layers) — Persistence restore routes through `request_step_transition()` from authority; mirrors, does not originate
- [x] Derived layers synchronize AFTER authority commit — `retry_step` and `stop_workflow` invalidate projection BEFORE lifecycle write, then regenerate AFTER persistence sync
- [x] No derived layer bootstraps authority from its own state — `warm_registry_from_disk()` validates before restoration; `inject_authoritative_lifecycle_into_workflow()` overwrites with registry truth
- [x] Cold-start quarantines stale state — `validate_workflow_recovery()` performs structural integrity checks; corrupt workflows quarantined
- [x] Stale-state quarantine prevents old snapshots overwriting authority — `validate_workflow_recovery()` checks missing step IDs, cyclic dependencies, schema violations; `quarantine_workflow()` marks QUARANTINED
- [~] Persistence durability guarantees are met — atomic file update used; durability partially verified
- [~] Snapshot semantics are correct — checkpoints exist; semantics not fully documented
- [~] Reconstruction sequencing is deterministic — restore sequencing exists; needs verification
- [~] Invalidation of persistence is handled correctly — not explicitly verified
- [~] Future-state durability direction is defined — `PERSISTENCE_AND_DURABILITY_CONTRACT_V1` defines direction; implementation unclear
- [x] Durable replay continuity operationalized — append-only chronology persistence verified
- [x] Restart-safe chronology restoration operationalized — replay continuity survives restart/reconnect
- [x] Replay continuity remains non-authoritative — chronology infrastructure preserves observability only
- [x] Continuity-aware hydration operationalized — append-only replay hydration continuity verified
- [x] Session restoration remains downward-convergent from runtime authority
- [x] Reconnect hydration does NOT synthesize lifecycle authority
- [x] Projection continuity during reconnect remains non-authoritative
- [x] Temporary reconnect divergence classified as legal eventual consistency
- [x] pre-authoritative workflow shell persistence operationalized for planning continuity survivability
- [x] planning-phase recovery continuity operationalized through authoritative pre-registration stabilization
- [x] recovery continuity no longer depends on delayed planner materialization timing

**Done When:**
- Recovery restores without replay
- Convergence is downward-only
- Stale state is quarantined on cold start

**Drift Detection:**
- Recovery replays execution
- Persistence overrides lifecycle authority
- Upward convergence exists

---

## PHASE 8 — PROJECTIONS, STREAMS & CONTINUITY

**Goal:** Projections are read-only. Streams transport only. Frontend hydrates downward.

**Status:** VERIFIED — projection continuity, replay chronology visibility, transport-agnostic replay cursor semantics, and render-only virtualization operationally validated

**Reference Documents:**
- PROJECTION_CONTINUITY_CONTRACT_V1.txt
- CANONICAL_PROJECTION_MODEL_V1.txt
- DERIVED_STATE_INVALIDATION_CONTRACT_V1.txt
- LIFECYCLE_AND_PROJECTION_AUTHORITY_CONTRACT_V1.txt

**Checklist:**

- [x] Projections NEVER synthesize lifecycle truth — `projection_manager.py` docstring: "MUST NOT own lifecycle authority, MUST NOT synthesize execution truth"
- [x] Projections NEVER overwrite authoritative lifecycle state — projection emission is read-only; no write path to lifecycle registry
- [x] Projections are read models / cached views only — `ProjectionManager` manages projection stores
- [x] Projection identity and versioning are correct — `projection_versions.json` exists; monotonic versioning enforced; stale rejection active
- [x] Projection emission follows authority commit — `retry_step` invalidates projection BEFORE `_update_workflow_state`, then `save_workflow`, then `emit_plan_mutated`
- [x] Projection invalidation is deterministic — `invalidate_workflow()` marks stale; `emit_plan_mutated()` regenerates deterministically
- [x] Stale projections trigger refresh — `load_from_persisted()` validates against runtime authority; stale persisted projections rejected
- [x] Stream/registry stores ONLY projections, NOT lifecycle truth — `_stream_registry` caches projection state; `_workflow_state_registry` holds lifecycle truth
- [x] Frontend derives ALL state from projections — `WorkflowProjectionView` renders projection only; `isExecuting` is parent-controlled prop
- [x] Frontend NEVER synthesizes lifecycle state — Audit verified: derives ALL state from projection, no local synthesis
- [x] Hydration ordering is projection → frontend — `WorkflowProjectionView` receives projection prop; `PlanMutationPanel` waits for canonical projection refresh
- [x] Downward-only convergence is enforced — Authority flow: Registry → Persistence → Projection → Stream → Frontend. Verified no upward reconciliation paths.
- [x] Transport layers are transport-only — event bus / emitter are advisory; polling transports projection only; no lifecycle ownership
- [x] Projection synchronization follows orchestration sequencing — projection invalidation precedes lifecycle write per SYSTEM_CONVERGENCE_AND_RECOVERY_CONTRACT_V1 §8
- [x] Projection mutation flow is legal — mutation invalidates projection, then lifecycle changes, then deterministic regeneration
- [x] bus_sequence_id chronology cursor operationalized — authoritative replay cursor semantics verified
- [x] since_sequence replay continuity operationalized — reconnect-safe replay hydration verified
- [x] Replay continuity semantics remain transport-agnostic — chronology continuity independent from polling/websocket/SSE
- [x] Timeline virtualization remains render-only — chronology authority and replay semantics unchanged
- [x] Reconnect projection hydration continuity operationalized
- [x] Refresh/reattachment continuity operationally validated
- [x] Frontend restoration remains projection-derived only
- [x] Reconnect continuity preserves transport-agnostic replay semantics
- [x] Terminal FAILED projection enrichment operationalized — projection exposes failure_reason, failed_step_id, failed_step_label, retry_target_step_id, last_successful_output, and last_successful_step_id where available
- [x] Terminal FAILED stream-to-projection convergence operationalized — live terminal FAILED display now hydrates projection-enriched failure metadata
- [x] FAILED refresh reattach projection continuity verified — focused FAILED projection survives refresh and rehydrates as projection-only view
- [x] Retry target projection metadata remains non-authoritative — backend/runtime computes retry target; projection exposes it as downstream read model only

Clarification: Terminal FAILED reattach does not resume execution and does not create replay semantics. It is projection-only hydration continuity.

**Clarification (2026-05-17):**
Eventual consistency semantics validated: temporary projection lag during pause/resume/mutation is acceptable. Permanent divergence (failure to converge) is the bug condition. Projection corruption NOT observed in runtime testing.

Additional replay-driven projection clarification (2026-05-27):

* replay-driven recovery validation exposed bounded projection metadata reconciliation drift after recovery hydration
* recovered workflows can retain stale derived title/summary metadata while lifecycle authority remains correct
* execution continuity, pause/resume continuity, and runtime identity continuity remain unaffected
* remaining concern classified as projection-derived metadata recomputation refinement only
* replay-driven validation confirmed projection synchronization gaps do NOT imply authority corruption
* projection_not_found reconciliation tolerance operationalized for planning-phase projection convergence
* foreground attachment continuity now tolerates temporary absence of canonical projections during planning initialization
* projection convergence and workflow identity continuity are now explicitly separated operational semantics

**Done When:**
- Projections are read-only
- Streams transport only
- Frontend hydrates from projections
- No upward convergence

**Drift Detection:**
- Projections override authority
- Frontend infers lifecycle state
- Upward reconciliation exists

---

## PHASE 9 — GUI & USER INTERACTION

**Goal:** GUI is a thin interaction layer. It cannot violate runtime legality. It renders projections.

**Status:** MOSTLY VERIFIED — WorkflowStudio operational governance stabilized, timeline observability operationalized, execution-feedback continuity operationalized, remaining work primarily ergonomic refinement

**Reference Documents:**
- GUI_ARCHITECTURE.txt
- GUI_FUNCTIONALITY_CONTRACT_V1.txt
- PLAN_CONTROL_CONTRACT_V1.txt

**Checklist:**

- [x] GUI actions are context-scoped — `user_control.py` / `user_approval.py` enforce context scoping
- [x] GUI respects mutation legality boundaries — backend `validate_workflow_mutable` and `validate_step_mutable` enforce legality; frontend `PlanMutationPanel` hidden for terminal workflows; `disabled={isExecuting}` locks controls during execution
- [x] GUI action sequencing follows orchestration ordering — mutation intents dispatch to `plan_mutation_manager`; no optimistic local state mutation; frontend waits for canonical projection refresh
- [x] GUI renders projections only — `projection_manager.py` serves projections to GUI
- [x] Workflow-scoped pause/resume — operationally verified via Playwright runtime validation
- [x] Projection-only frontend rendering — operationally verified; no lifecycle synthesis detected
- [x] Multi-workflow continuity — operationally verified; no cross-workflow contamination detected
- [x] Mutation legality continuity — operationally verified; ACTIVE edits blocked, terminal step locking preserved
- [x] Retry lineage continuity — operationally verified
- [x] Recovery continuity — operationally verified
- [x] Lifecycle synthesis elimination — verified; frontend derives ALL state from projection
- [x] Projection continuity — operationally verified via Playwright validation
- [~] Override control — LEGACY ARTIFACT / REMOVAL EVALUATION; classified as pre-redesign governance artifact
- [~] Recoverability semantics refinement — PARTIAL; Task Hub derivation remains heuristic
- [~] GUI shows mutation legality (what is legal/illegal) — not explicitly verified
- [x] GUI never assumes synchronization completion authority — frontend waits for canonical projection refresh; does not optimistically assume mutation completion
- [x] GUI never synthesizes lifecycle truth — Authority Reconciliation Audit (Phase S2) verified: `isExecuting` is parent-controlled prop; `workflowId` derives from projection; no local synthesis (ISSUE_034: FALSE POSITIVE)
- [x] GUI never restores lifecycle state from projections — frontend derives display state from projection prop only; no projection-to-lifecycle restoration path exists
- [x] Plan control actions are validated — `plan_mutation_manager.py` validates plan mutations
- [~] Workflow interaction is safe (no bypass) — partially; needs adversarial test
- [x] Workflow reattachment continuity operationally validated
- [x] ACTIVE continuity restoration operationally validated
- [x] RuntimeActivity downward convergence operationally stabilized
- [x] Task Hub reattachment survivability operationally validated
- [x] GUI consumes authoritative retry_target_step_id for failed-step retry
- [x] GUI does not infer retry target from arbitrary BLOCKED steps
- [x] Execution Result separates failure metadata from last successful output
- [x] FAILED workflow foreground reattachment after refresh operationally verified
- [x] Foreground refresh continuity for CANCELLED inspection-only workflows verified
- [x] Foreground refresh continuity for COMPLETED inspection-only workflows verified
- [x] Foreground attachment separated from Task Hub membership
- [x] Task Hub exclusion does not detach foreground workflow
- [x] Task Hub excludes CANCELLED inspection-only workflows
- [x] Task Hub excludes COMPLETED inspection-only workflows
- [x] Task Hub preserves FAILED workflow visibility
- [x] Task Hub preserves PAUSED workflow visibility
- [x] Task Hub preserves PENDING_RECOVERY workflow visibility
- [x] PENDING_RECOVERY Task Hub recovery visibility verified after app close/reopen
- [x] FAILED / RECOVERABLE / Projection TERMINAL WorkflowStudio display verified
- [~] Multi-project context is handled correctly — `projects/` exists; multi-project handling unclear
- [~] GUI error handling is projection-aligned — needs verification

[CURRENT PRIORITY DIRECTION]

Immediate GUI refinement priorities are:

- ISSUE-057 terminal FAILED surface convergence core flow verified
- remaining GUI work is now limited to operational cleanup, Task Hub failed-workflow clutter, backend lifecycle hardening follow-up, and future failed-workflow history/archive design
- UNKNOWN lifecycle cleanup
- runtime activity observability visibility
- execution-feedback continuity during delayed workflow startup
- authoritative operator activity visibility continuity
- workflow/projection visual clarity
- PLAN/editor consolidation
- dashboard vs focused workflow separation
- operator UX readability refinement

Current focus is operator-facing clarity and observability refinement.

Current focus is NOT:
- adaptive orchestration
- replay architecture
- runtime self-healing
- unrestricted live workflow mutation

[ARCHITECTURE CLARIFICATION]

Plan editing semantics formally restricted and stabilized.

Verified architecture rules:
- ACTIVE execution remains immutable
- Mutation legality windows enforced
- Canonical projections remain orchestrator-owned
- Projection continuity remains non-authoritative
- Frontend remains thin interaction layer only
- Dashboard observability remains passive visibility layer only
- Focused workflow interaction remains explicit attachment surface only

Future adaptive reconciliation and branch-healing behavior moved into future-state adaptive orchestration contracts.

Current implementation remains deterministic execution oriented.

[x] GUI interaction surfaces reconciled with mutation legality architecture — governance-aware legality visibility operationalized
[x] PLAN interaction model reconciled with deterministic execution model — WorkflowStudio operational interaction model stabilized
[x] Dashboard observability layer separated from focused workflow interaction layer — focused workflow attachment semantics operationalized
[x] Deferred/rejected mutation UX behavior clarified — structured governance rejection UX operationalized
[x] Runtime activity observability continuity implemented — submission lifecycle visibility, delayed startup observability continuity, and execution-feedback persistence operationally validated
[ ] UNKNOWN lifecycle rendering cleanup completed (operator UX refinement only — NOT lifecycle authority instability)
[x] Projection vs runtime visual authority hierarchy clarified — timeline/sidebar observability and projection-derived rendering operationalized
- [x] Retry lineage visibility operationalized
- [x] Execution-generation visibility operationalized
- [x] Transition chronology visibility operationalized
- [x] Render-only timeline virtualization operationalized
- [x] Cancellation governance UX operationalized
- [x] WorkflowStudio operational governance stabilization verified

Current GUI architecture assumes:

- deterministic execution
- orchestrator-owned projections
- passive dashboard observability
- workflow-scoped interaction surfaces
- no frontend lifecycle synthesis
- no adaptive branch healing semantics

Additional verified GUI/runtime clarification (2026-05-19):

* cooperative pause semantics may temporarily freeze dependency-chain projections
* frozen BLOCKED dependency rendering during PAUSED is valid eventual-consistency behavior
* runtime convergence after resume remains the authoritative correctness condition
* temporary UNKNOWN lifecycle fallback rendering is classified as UX debt rather than orchestration instability
* projection timing divergence during cooperative orchestration is acceptable unless convergence permanently fails

Additional replay-driven GUI clarification (2026-05-27):

* planning-phase refresh continuity materially stabilized through backend authoritative pre-registration and QUEUED eligibility reconciliation
* refresh during planning may preserve backend execution while foreground workflow attachment temporarily falls back to Task Hub visibility
* Task Hub recovery and reattachment continuity remain operationally correct after planning continuity loss
* replay-driven validation materially improved operator-visible continuity validation precision
* frontend attachment continuity and runtime execution continuity are operationally distinct validation surfaces
* foreground attachment continuity now survives refresh during planner initialization windows
* planning-phase operator continuity now tolerates temporary projection absence without foreground detachment
* reattachment UX continuity operationalized during pre-projection planning windows
* backend execution continuity and foreground attachment continuity formally reconciled as distinct operational semantics
* replay-driven validation confirmed planning continuity failures originated from delayed authoritative identity materialization rather than lifecycle authority corruption

**Done When:**
- GUI is interaction-only
- No action violates legality
- GUI never creates lifecycle truth

**Drift Detection:**
- GUI triggers illegal mutations
- GUI infers lifecycle state from projections
- GUI assumes synchronization authority

[NOTE]

Adaptive reconciliation, preserved-valid-work continuation, branch healing, and intelligent execution graph repair are future-state architecture concerns governed by:
- ADAPTIVE_RUNTIME_ORCHESTRATION_CONTRACT_V1
- HUMAN_IN_THE_LOOP_WORKFLOW_RECONCILIATION_V1

These behaviors are NOT part of the current deterministic runtime implementation baseline.

---

## PHASE 10 — VALIDATION, OBSERVABILITY & HARNESS

**Goal:** The system can be tested. Correctness is observable. Harness validates contracts.

**Status:** MOSTLY VERIFIED — runtime survivability validation, replay continuity validation, chronology visibility validation, and operational governance validation operationalized

**Reference Documents:**
- HARNESS_SPEC.txt
- VALIDATION_ARCHITECTURE.txt
- TRACE_LOGGING_CONTRACT_V1.txt
- TRACE_MODEL.txt

**Checklist:**

- [x] Harness exists and validates core system behavior — `harness.py` and `harness_v2.py` exist
- [x] Harness validates lifecycle transitions — `test_lifecycle_durability.py`, `test_lifecycle_authority_hotfix.py` provide coverage; runtime survivability additionally validated via Playwright
- [x] Harness validates execution sequencing — `test_execution_ownership_coordination.py` validates execution generation and ownership; Playwright concurrent execution stress validates orchestrator sequencing
- [x] Harness validates mutation legality — mutation legality enforced at runtime; Playwright `active_mutation_continuity.spec.ts` and `edit_mode_legality.spec.ts` validate mutation behavior
- [x] Harness validates recovery behavior — `test_execution_recovery_stabilization.py` covers all three confirmed live failure modes; `test_lifecycle_durability.py` covers crash recovery
- [x] Harness validates projection continuity — `test_projection_continuity.py`, `test_canonical_projection.py` provide coverage
- [~] Harness validates convergence direction — downward-only convergence verified INDIRECTLY through component tests; no single end-to-end invariant test exists
- [x] Trace logging captures execution events — `trace_collector.py` with 26KB of trace logic
- [x] Trace model is consistent — trace files in `traces/` follow consistent schema
- [x] Validation architecture separates harness from internal tests — conceptual separation preserved in test design; actual test files reside in `tests/` root. `tests/harness/` and `tests/harness_cases/` exist but are empty. `system/tests/` does not exist. This is organizational/documentation drift, NOT missing validation.
- [~] Observability captures authority layer behavior — `drift_detector.py` exists; authority-layer observability partial
- [~] Drift detection exists in observability — `drift_detector.py` exists; needs verification
- [x] Playwright runtime survivability regression coverage exists — deterministic runtime survivability suite operational in `frontend/tests/e2e/`
- [x] Pause/resume continuity validated under real runtime execution — runtime registry continuity validated through Playwright survivability coverage
- [x] Mutation replay continuity validated — execution_generation invalidation and retry lineage continuity validated
- [x] Projection convergence validated under mutation/retry — authoritative runtime convergence validated with projection continuity checks
- [x] Workflow isolation validated under concurrent execution — concurrent workflow survivability and scoped runtime recovery validated
- [x] Stale-owner suppression validated — cooperative orchestration invalidation validated through execution_generation inspection
- [x] Cooperative pause boundary semantics validated — active-step completion before pause convergence operationally verified
- [x] Restart continuity validated — workflow recovery continuity validated across restart/reconnect boundaries
- [x] Event continuity validated — stream/projection continuity validated under orchestration mutation and recovery operations
- [x] Semantic lifecycle observability validated — semantic invalidation, regeneration, and advisory isolation runtime-verified
- [x] Semantic authority isolation validated — semantic systems confirmed downstream from execution authority
- [x] Semantic replay authority absent by design — no semantic replay or authority restoration architecture introduced
- [x] Runtime startup observability continuity validated
- [x] Execution-feedback continuity validated during delayed workflow attachment
- [x] Operator-facing execution visibility validated during long-running planning phases
- [x] Runtime smoke validation operationalized for GUI stabilization/refactor work
- [x] Durable replay continuity validated under restart/reconnect
- [x] Replay chronology continuity validated
- [x] Retry lineage continuity validated
- [x] Cancellation semantic continuity validated
- [x] Replay cursor continuity validated
- [x] Timeline/sidebar chronology visibility validated
- [x] Render-only virtualization continuity validated

**Clarification (Playwright Regression Coverage):**

Playwright runtime validation is used for orchestration survivability and convergence validation — NOT cosmetic frontend testing.

Additional replay-driven validation maturity clarification (2026-05-27):

* screenshot/evidence-driven operational validation materially improved replay semantic precision
* passing Playwright assertions alone are insufficient operational continuity proof
* replay validation now explicitly validates:
  - foreground attachment continuity
  - Task Hub persistence continuity
  - operator-visible recovery continuity
  - projection convergence continuity
  - workflow ownership continuity
* replay validation maturity materially increased during Sprint 2 operational replay hardening
* backend lifecycle continuity and operator-visible continuity are now explicitly distinguished validation domains
* planning-phase refresh continuity validation substantially expanded during Sprint 2
* replay-driven validation now explicitly covers:
  * pre-registration continuity
  * planning-window refresh survivability
  * projection_not_found recovery tolerance
  * foreground attachment continuity during projection absence
  * QUEUED lifecycle recovery continuity
* replay-driven evidence collection materially improved operational synchronization diagnosis precision

Sprint 2 ISSUE-057 validation now includes manual evidence-driven validation of:
- divide-by-zero terminal failure
- failed-step preservation
- downstream blocked preservation
- retry target exposure
- enriched failure-result display
- failed workflow refresh reattachment

Automated validation passed according to implementation reports:
- validate_issue_057_fixes.py passed
- validate_issue_057.py passed
- issue_057_terminal_failed_convergence.spec.ts passed

Note: Flaky pause/edit/resume Playwright behavior remains classified as pre-existing flaky timing behavior, not a new Sprint 2 regression.

Runtime survivability validation now includes deterministic runtime authority inspection via read-only observability endpoints.

Validated runtime surfaces include:

* execution_generation invalidation
* stale-owner suppression
* retry lineage continuity
* runtime registry lifecycle continuity
* projection convergence continuity
* concurrent workflow isolation
* restart/recovery continuity

Projection lag during ACTIVE execution is legal eventual-consistency behavior.

Permanent divergence or failed convergence is the actual failure condition.

Frontend rendering remains downstream from authoritative runtime state.

**Test Location Reference:**
- `frontend/tests/e2e/` — runtime regression suite
- `frontend/tests/e2e/` — orchestration continuity validation
- `frontend/tests/e2e/` — lifecycle survivability validation

**Done When:**
- All critical paths have automated validation coverage
- Traces capture execution behavior
- Validation architecture documentation matches actual test locations
- CI/build validation pipeline operational
- Playwright runtime survivability suite operational

**Drift Detection:**
- Tests pass but runtime behavior differs (indicates test quality issue, not architecture issue)
- Critical paths lack ANY validation (runtime + Playwright + manual)
- No observability into authority layers

**Important:** Missing automated tests do NOT imply runtime instability. The system is operationally verified through manual testing, Playwright runtime proving, and adversarial validation. Gaps in automated repeatability are production-readiness concerns, not runtime-corruption concerns.

---

## PHASE 11 — STABILIZATION & HARDENING

**Goal:** Edge cases are handled. Races are eliminated. Drift is remediated.

**Status:** OPERATIONALLY STABLE — authority stabilization, replay continuity stabilization, governance reconciliation, and render scalability stabilization operationally verified

**Reference Documents:**
- All core contracts
- ARCHITECTURE_LAW.txt
- KNOWN_ISSUES_V2.txt

**Checklist:**

- [~] All edge cases from contracts are handled — major edge cases validated; minor edge-case hardening remains future work
- [x] Synchronization race-adjacent edge cases operationally mitigated — cooperative orchestration semantics verified stable; no destabilizing runtime corruption observed
- [~] Orphan handling works under failure — partially validated; full adversarial orphan coverage is future hardening
- [x] Recovery survives failure during recovery — `test_execution_recovery_stabilization.py` validates recovery continuity; Playwright recovery/restart continuity validated
- [x] Projection vs authority timing races are fixed — projection lag during mutation/pause/resume is legal eventual consistency; permanent convergence failure is the actual bug condition
- [x] Runtime drift is identified and remediated — drift detection exists (`drift_detector.py`); automated drift detection validation coverage is future work
- [ ] All drift categories are audited:
  - [x] Authority Drift — Authority Reconciliation Audit (Phase S2) COMPLETE. Verdict: PARTIALLY ALIGNED. NO critical authority collapse. Runtime registry correctly serves as lifecycle authority per contract. NO projection authority leakage. NO persistence authority inversion.
  - [x] Sequencing Drift — audited: orchestrator loop deterministic; execution_scheduler sequences correctly; governance decision pipeline deterministic
  - [x] Convergence Drift — Audit verified: downward-only convergence correctly implemented (Registry → Persistence → Projection → Stream → Frontend)
  - [x] Recovery Drift — audited: warm_registry_from_disk deterministic; quarantine discipline active; PENDING_RECOVERY fallback prevents cold-start authority gaps
  - [x] Mutation Drift — audited: derived artifact invalidation verified in edit_step, retry_step, plan_mutation_manager; semantic propagation remains partial (scaffolding only)
  - [x] Projection Drift — audited: projection continuity verified; eventual consistency semantics validated; temporary lag accepted; corruption not observed
  - [x] Runtime Coordination Drift — audited: execution_generation stale-owner suppression active; cooperative pause boundaries verified; BLOCKED→PAUSED rejection validated
  - [x] Persistence Drift — audited: persistence correctly mirrors downward-only; atomic writes implemented; no authority inversion observed
- [x] Remediation does not introduce new drift — no regression detected across recent stabilization phases
- [x] Foreground refresh continuity for immutable terminal inspection workflows operationally verified
- [x] CANCELLED foreground refresh inspection continuity operationally verified
- [x] COMPLETED foreground refresh inspection continuity operationally verified
- [x] Task Hub / foreground attachment semantic separation operationally verified
- [x] Task Hub immutable terminal inspection-only exclusion operationally verified
- [x] Task Hub preserves FAILED, PAUSED, and PENDING_RECOVERY visibility after terminal exclusion filter
Clarification (2026-05-17):
Recent runtime audits verified:

* cooperative orchestration semantics are intentional architecture
* execution_generation stale-owner suppression operational
* derived artifact invalidation operational
* retry replay safety operational
* projection convergence ordering operational
* terminal semantics operationally complete
* GUI projection purity operational
* workflow-scoped event isolation operational
* semantic lifecycle classification reconciled
* advisory semantic isolation operational
* semantic invalidation lifecycle-safe
* semantic observability runtime-verified

Remaining stabilization work is primarily:

* edge-case hardening
* semantic signal-quality refinement
* observability refinement
* startup authority consolidation
* execution-feedback continuity refinement
* operator-facing runtime visibility clarity
* production maturity
* advanced adversarial validation
* replay semantic refinement
* foreground continuity refinement
* projection metadata reconciliation refinement
* operational synchronization refinement
* operator-visible continuity hardening

Clarification:

Replay-driven operational validation clarified:

* bounded synchronization gaps remain active Sprint 2 work
* projection reconciliation roughness remains active Sprint 2 work
* remaining issues are operational synchronization maturity concerns
* runtime authority architecture remains stable
* orchestration authority remains stable
* lifecycle authority remains stable

The system is no longer classified as undergoing orchestration stabilization or authority rescue.

Remaining stabilization work is refinement/hardening only:

- semantic refinement
- observability clarity
- adversarial validation
- production maturity
- projection convergence refinement during delayed projection initialization
- operational synchronization edge-case hardening during planning initialization
- replay evidence instrumentation refinement
- operational semantic/UI interpretation refinement
- bounded projection-label clarity refinement
- operator-visible terminology clarification
- regression-risk minimization during synchronization polish

NOT:
- authority rescue
- orchestration redesign
- lifecycle ownership correction

Additional ISSUE-056 operational clarification (2026-05-28):

* runtime authority continuity remains stable
* projection convergence remains bounded eventual consistency
* remaining ACTIVE vs PAUSED ambiguity is classified as semantic/operator interpretation refinement rather than lifecycle authority instability
* projection transport/activity continuity may remain ACTIVE while workflow execution lifecycle is PAUSED
* continued synchronization churn beyond bounded refinement carries increasing regression risk with diminishing architectural value
* ISSUE-056 is now classified as bounded operational polish rather than authority remediation

Additional Sprint 2 ISSUE-057 clarification (2026-05-29):

* failed-workflow lifecycle surface synchronization core flow is functionally verified
* causative failed steps now preserve FAILED status
* downstream dependency-victim steps remain BLOCKED
* backend-authored retry target metadata is exposed and consumed by GUI
* failure-result display now surfaces failure reason, failed step, retry target, and last successful output separately
* focused FAILED workflow refresh reattachment is projection-only and does not resume execution
* frontend remains non-authoritative
* projection remains non-authoritative
* retry legality remains backend/runtime governed

Separate follow-up items, not ISSUE-057 core blockers:

* old failed workflows still clutter Task Hub
* permanent `src-tauri/src/lib.rs` backend lifecycle hardening is still required so stale/ghost backend processes cannot survive app closure, cause port collisions, keep executing workflows, or terminalize workflows unexpectedly
* future failed-workflow history/archive design may replace keeping terminal failed workflows in active Task Hub

Additional Sprint 2 clarification (2026-05-28):

* backend authoritative pre-registration materially stabilized planning continuity semantics
* remaining synchronization concerns are now bounded edge-case refinement only
* no remaining evidence of lifecycle authority instability
* no remaining evidence of orchestration authority fragmentation
* planning continuity failures were conclusively traced to delayed authoritative identity materialization timing

- [x] Re-audit passes for affected domains — Phase A meta-audit, Phase B architectural reconciliation, and Phase C1 harness coverage audit all completed without finding runtime corruption or authority collapse

Additional Operational Maturity Clarification (2026-05):

Recent stabilization work additionally verified:

* WorkflowStudio operational governance stabilization
* cancellation semantic reconciliation
* durable replay continuity operationalization
* append-only chronology hydration stabilization
* transport-agnostic replay cursor stabilization
* render-only timeline virtualization stabilization

Remaining work is now primarily:

* ergonomic refinement
* production readiness
* adversarial coverage expansion
* operational scaling refinement
* recoverability interpretation refinement

The system is no longer undergoing foundational authority reconciliation or replay continuity rescue work.

**Done When:**
- All known edge cases handled
- No synchronization races
- All critical drift remediated
- Re-audit passes

**Drift Detection:**
- Edge cases cause crashes
- Races produce non-deterministic behavior
- Remediation introduces new drift

---

## PHASE 12 — PRODUCTION READINESS

**Goal:** System is operationally safe. Deployment is hardened. Monitoring exists.

**Status:** AUDITED (Phase C5) — Production readiness gaps identified and classified as operational maturity deficiencies, NOT runtime architecture defects. Architecture is stable; hardening is incomplete.

**Reference Documents:**
- SANDBOX_PRODUCTION_CONTRACT_V1.txt
- SYSTEM_GOALS_V2.txt
- ARCHITECTURE_LAW.txt

**Checklist:**

- [ ] Contract validation runs automatically on every build — CI pipeline absent; manual test execution currently required
- [ ] Performance baseline is established (timing, memory, throughput) — no performance tests exist
- [ ] Monitoring detects execution health — no monitoring infrastructure exists
- [ ] Monitoring detects drift — no automated drift detection observability exists
- [ ] Anomaly alerting exists — no alerting infrastructure exists
- [ ] Operational tooling supports safe debugging
- [ ] Safe restart procedure exists
- [ ] Deployment supports safe rollout
- [ ] Deployment supports safe rollback
- [ ] Sandbox environment is configured
- [ ] Production environment boundaries are defined
- [ ] System goals are validated against implementation
- [ ] Architecture law is enforced in production
- [ ] API authentication implemented — no auth on any endpoint
- [ ] CORS restricted to known origins — currently `allow_origins=["*"]`
- [ ] Debug/admin endpoints disabled in production — `/debug/events`, `/debug/control_state`, `/admin/test/reset_runtime` exposed
- [ ] `/health` endpoint operational — startup script references `/health` but API only provides `/status`
- [ ] MODE configured for production — `core/config.py` defaults to `"debug"`

**Done When:**
- Automated validation on build
- Performance acceptable
- Monitoring and alerting active
- Deployment is safe

**Drift Detection:**
- No automated validation
- Performance unknown
- No monitoring
- Unsafe deployment practices

---

## PHASE 13 — INTELLIGENCE LAYER & FUTURE EXPANSION

**Goal:** Intelligence layer is ready for future expansion. Agent behavior is bounded. Planner evolution is safe.

**Status:** NOT STARTED — dual-planner architecture exists but not formally locked

**Reference Documents:**
- INTELLIGENCE_LAYER_PLAN.txt
- AGENT_CONTRACT_V1.txt
- SEMANTIC_EXPECTATION_MODEL_CONTRACT_V1.txt
- HAND_ARCHITECTURE_V2.txt

**Checklist:**

- [~] Agent behavior boundaries are defined — `AGENT_CONTRACT_V1.txt` exists; implementation boundaries unclear
- [~] Agent does not override runtime contracts — `agent_executor.py` exists; needs audit
- [x] Planner contract supports dual-planner architecture — `core/planner.py` + `system/orchestrator/orchestrator_planner.py` exist
- [x] Core planner is deterministic — `system/planner/deterministic_planner.py` is deterministic
- [x] Orchestrator planner is properly isolated — `orchestrator_planner.py` sits above `system_entry`
- [~] No cross-layer planner access — `core/planner/` has `llm_adapter.py`; potential cross-layer concern
- [~] Semantic expectation model is defined — `semantic_expectation.py` exists; needs verification
- [~] Semantic validation relationship is clear — `intent_validator.py` exists; relationship needs audit
- [ ] Intelligence layer plan is ready for execution — not verified
- [ ] Future architecture expansion does not destabilize current system — not verified
- [ ] Self-improvement loop is bounded by contracts — not implemented
- [ ] Sandbox supports intelligence layer experimentation — not verified

**Done When:**
- Agent behavior is bounded
- Planner architecture is stable
- Intelligence layer plan is ready
- Future expansion is safe

**Drift Detection:**
- Agent overrides runtime authority
- Planners cross layers
- Intelligence layer destabilizes execution

---

## FINAL AUDIT SUMMARY

**When all phases are complete:**

- [x] PHASE 1: Core System — VERIFIED
- [x] PHASE 2: Lifecycle Authority — VERIFIED
- [x] PHASE 3: Execution Structure — VERIFIED 
- [x] PHASE 4: Orchestration & Coordination — VERIFIED (runtime survivability operationally validated)
- [x] PHASE 5: Execution Identity — VERIFIED (execution lineage continuity and replay chronology continuity operationalized; replay engine intentionally absent)
- [x] PHASE 6: Mutation Legality — VERIFIED (operational governance visibility and cancellation legality operationalized)
- [x] PHASE 7: Recovery & Convergence — VERIFIED (durable replay continuity and restart-safe chronology restoration operationalized)
- [x] PHASE 8: Projections & Continuity — VERIFIED (replay cursor stabilization and render-only virtualization operationalized)
- [x] PHASE 9: GUI & Interaction — MOSTLY VERIFIED (WorkflowStudio operational governance stabilized; remaining work primarily ergonomic refinement)
- [x] PHASE 10: Validation & Observability — VERIFIED FOR RUNTIME CORRECTNESS (runtime survivability, replay continuity, chronology visibility, and governance validation operationalized). Production-readiness validation (CI, performance baselines, monitoring) NOT STARTED.
- [x] PHASE 11: Stabilization & Hardening — VERIFIED (operational maturity stabilization checkpoint achieved). Remaining work is refinement and production hardening, not authority rescue.
- [ ] PHASE 12: Production Readiness — AUDITED (Phase C5 complete). Gaps identified as operational maturity, not runtime instability.
- [~] PHASE 13: Intelligence Layer — PARTIALLY VERIFIED (dual-planner exists; Sprint 5 AG1 Baseline Audit is next direction; AG1 not yet implemented)

**System is COMPLETE when all 13 phases are verified.**

---

## OPERATIONAL MATURITY CHECKPOINT (May 2026)

The system has completed foundational operational maturity stabilization.

Verified stabilization outcomes include:

- authority stabilization
- projection stabilization
- governance reconciliation
- retry semantic reconciliation
- cancellation semantic reconciliation
- WorkflowStudio operational governance stabilization
- durable replay continuity stabilization
- transport-agnostic replay cursor stabilization
- render-only replay virtualization stabilization

### Sprint 1 — Operational Survivability Hardening (COMPLETE)

- [x] Sprint 1 survivability hardening completed
- [x] retry lineage observability enriched (per-step metadata in runtime inspect)
- [x] reconnect test determinism hardened (registry-based discovery, runtime inspect validation)
- [x] mutation defensive hardening (embedded failure status surfacing)
- [x] bootstrap lifecycle label hardening (ACTIVATING, PENDING_RECOVERY)
- [x] resurrection execution guard hardening (isExecuting covers ACTIVATING, PENDING_RECOVERY)
- [x] manual operational survivability validation completed

Verified stable behaviors:
- refresh survivability
- reconnect survivability
- pause/resume continuity
- cancellation cleanup
- FAILED recovery
- mutation legality
- future-step editing
- retry/recovery flows
- backend execution independence
- workflow survivability after frontend closure
- projection recovery
- chronology continuity
- workflow completion correctness

### Sprint 2 — Operational Synchronization Hardening (PENDING)

Targets:
- [x] planning continuity anchoring
- [x] planning-phase foreground continuity refinement
- [x] backend authoritative pre-registration continuity stabilization
- [x] QUEUED lifecycle foreground eligibility reconciliation
- [x] projection_not_found planning tolerance reconciliation
- [x] planning-window foreground attachment survivability operationally validated
- [x] pre-authoritative workflow identity continuity operationally stabilized
- [ ] runtimeActivity downward convergence
- [x] task hub freshness synchronization materially stabilized
- [x] attach/detach convergence continuity materially stabilized
- [x] reattachment lifecycle synchronization operationally stabilized
- [x] FAILED terminal-state synchronization convergence core flow verified
- [x] FAILED terminal-state projection convergence core flow verified
- [x] foreground refresh continuity for immutable terminal inspection workflows operationally verified
- [x] CANCELLED foreground refresh inspection continuity operationally verified
- [x] COMPLETED foreground refresh inspection continuity operationally verified
- [x] Task Hub / foreground attachment semantic separation operationally verified
- [x] Task Hub immutable terminal inspection-only exclusion operationally verified
- [x] Task Hub preserves FAILED, PAUSED, and PENDING_RECOVERY visibility after terminal exclusion filter
- [ ] projection metadata recomputation refinement after recovery hydration
- [ ] replay semantic hardening and evidence-driven validation refinement
- [x] ISSUE-062 FAILED actionability metadata skeleton implemented
- [x] retry_eligible backend guard validated
- [x] taskhub_eligible/history_eligible FAILED metadata validated
- [x] current actionable FAILED behavior preserved
- [x] terminal/non-actionable FAILED skeleton validated
- [ ] natural terminal/non-actionable FAILED governance/runtime path, if ever needed
- [x] ISSUE-060 archive/dismiss endpoints operationalized
- [x] retention_state persisted in workflow JSON
- [x] Task Hub filters archived/dismissed workflows
- [x] delete deferred
- [x] History/Archive UI deferred to ISSUE-061
- [ ] old polluted FAILED/BLOCKED workflow cleanup or archival strategy
- [x] Tauri backend lifecycle hardening (ISSUE-063) — backend instance / port ownership guard complete and validated; abnormal Windows process-tree cleanup remains optional follow-up
- [x] Failed-workflow history/archive design evaluation (ISSUE-061)

Classification: operational synchronization hardening only. No architecture redesign. No authority redistribution. No deterministic orchestration alteration.

Sprint 2 replay-driven operational validation confirmed:

* ACTIVE refresh continuity operationally stable
* app-close survivability operationally stable
* Task Hub recovery continuity operationally stable
* reattachment continuity operationally stable
* post-recovery execution continuation operationally stable

Remaining Sprint 2 work is concentrated in:
- operational synchronization edge-case refinement
- projection metadata reconciliation refinement
- terminal-state projection convergence refinement
- replay semantic hardening
- evidence-driven validation refinement
- operator-visible continuity refinement
- semantic/operator-visible lifecycle terminology refinement only where operationally valuable
- avoid unnecessary synchronization churn without evidence of permanent convergence failure

ISSUE-056 reconciliation clarification:

* temporary projection/activity ACTIVE visibility during PAUSED workflow states can be architecturally valid when editor/projection transport continuity remains active
* workflow execution lifecycle and projection/editor activity lifecycle are distinct operational semantics
* bounded eventual consistency remains acceptable architecture behavior
* permanent divergence is the actual defect condition
* semantic/operator interpretation ambiguity does NOT imply authority instability

NOT:
- authority redistribution
- orchestration redesign
- lifecycle redesign
- replay architecture replacement

Verified architecture characteristics:

- backend authority stabilized
- frontend remains projection-derived
- replay continuity remains non-authoritative
- chronology remains append-only
- replay continuity remains transport-agnostic
- causality synthesis intentionally deferred

Current operational posture:

- operationally stable
- synchronization semantics materially stabilized
- remaining synchronization work primarily semantic/operator-facing refinement
- authority architecture no longer under active reconciliation
- suitable for real workflow operational usage
- future evolution should be pressure-driven
- major infrastructure redesign currently unjustified

Additional replay-driven operational posture clarification:

* runtime-authority stability materially strengthened through replay-driven survivability validation
* operational replay maturity materially improved
* bounded synchronization edge cases remain active refinement work
* projection reconciliation refinement remains active refinement work
* replay validation quality materially improved through evidence-first operational validation

Intentionally deferred:

- websocket migration
- replay graph engines
- causality visualization
- advanced replay query infrastructure
- autonomous reconciliation systems

Replay-driven operational reconciliation confirmed:

* planning continuity instability was synchronization-timing related rather than authority-related
* backend lifecycle authority remained stable throughout ISSUE-055 investigation
* frontend continuity failures were caused by delayed authoritative workflow identity availability
* authoritative pre-registration resolved the primary continuity instability class without introducing heuristic restoration
* Task Hub recovery semantics and focused foreground continuity semantics are now explicitly separated and operationally stabilized

### Sprint 3 — Planning Recovery Baseline Restoration (COMPLETE)

- [x] ISSUE-055B planning_request persistence operationalized
- [x] QUEUED_REPLAN_REQUIRED actionability classification operationalized
- [x] Resume Planning / Replan endpoint operationalized
- [x] stale bg_id polling suppressed for interrupted planning
- [x] same workflow_id continuity verified across operator-initiated replan
- [x] ISSUE-061 Workflow Management Shell operationalized
- [x] Task Hub / History separation verified
- [x] /workflows/historical endpoint operationalized
- [x] History ordering by history_sort_timestamp/history_sort_source verified
- [x] History search/filtering verified
- [x] main workspace read-only History Inspector verified
- [x] Phase 5A read-only Trace/Event History visibility verified

### Sprint 3 — ISSUE-062 FAILED Actionability Metadata Skeleton (COMPLETE)

- [x] FAILED actionability metadata skeleton implemented
- [x] retry_eligible backend guard validated
- [x] taskhub_eligible/history_eligible FAILED metadata validated
- [x] current actionable FAILED behavior preserved by default
- [x] terminal/non-actionable FAILED skeleton validated
- [ ] natural terminal/non-actionable FAILED governance/runtime path, if ever needed

### Sprint 3 — ISSUE-063 Backend Instance / Port Ownership Guard (COMPLETE)

- [x] backend /identity endpoint implemented and validated
- [x] app_instance_id validation operationalized
- [x] stale/external/unrelated backend rejection validated
- [x] wrong-backend mismatch UI validated
- [x] no auto-kill of unknown external processes preserved

### Sprint 3 — ISSUE-065 Operational Mutation Validation (COMPLETE)

- [x] ISSUE-065 operational mutation validation completed
- [x] ACTIVE mutation rejection validated
- [x] PAUSED operational metadata edit validated
- [x] metadata refresh survival validated
- [x] metadata detach/reattach survival validated
- [x] metadata app close/reopen survival validated
- [x] resume after metadata edit validated
- [x] expected_outcome confirmed non-authoritative
- [x] terminal COMPLETED mutation guard validated
- [x] no implementation required

### Sprint 4 — Realtime Observability Transport & Projection Freshness Refinement (IN PROGRESS — READY FOR ISSUE-071)

- [x] Sprint 4 setup documented
- [x] ISSUE-067 Transport Baseline Audit completed
- [x] current websocket infrastructure audited
- [x] current polling transport behavior audited
- [x] current event delivery path audited
- [x] current projection freshness behavior audited
- [x] current reconnect/rehydration behavior audited
- [x] current duplicate event handling audited
- [x] current stale projection refresh trigger behavior audited
- [x] polling fallback legality verified
- [x] ISSUE-068A WebSocket/SSE risk + effort audit completed
- [x] SA roadmap sequencing review completed
- [x] push transport deferred by PO/Head Dev/SA decision
- [ ] ISSUE-068 WebSocket Reconnect + State Rehydration implemented — DEFERRED / NOT CURRENT SPRINT 4 CLOSURE REQUIREMENT
- [ ] ISSUE-068B SSE Event-Hint Spike implemented — DEFERRED / FUTURE OPTIONAL REASSESSMENT
- [x] ISSUE-069 Projection Freshness + Stale Refresh Triggers implemented — build-validated; partial manual regression validation complete
- [x] ISSUE-070 Duplicate Event Suppression + Polling Fallback Validation completed — PASS WITH OBSERVATIONS; no implementation required
- [ ] ISSUE-071 Transport Documentation + Regression Validation completed

### Sprint 5 — AG1 Baseline Audit / Architecture Validation (NOT STARTED)

- [ ] Sprint 5 AG1 Baseline Audit opened
- [ ] AG1 current-code baseline audited
- [ ] AG1 authority boundaries validated
- [ ] AG1 architecture validation completed
- [ ] AG1 initial operationalization approved
- [ ] Push transport reassessed after AG1 baseline / initial operationalization
- [ ] Optional ISSUE-068B SSE Event-Hint Spike reconsidered only if justified

---

END OF MASTER IMPLEMENTATION CHECKLIST
