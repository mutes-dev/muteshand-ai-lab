# SYSTEM_STATE.txt

## CURRENT TRUE STATE OF THE SYSTEM

**Last Updated:** April 2026  
**Status:** CORE HARDENING COMPLETE — ORCHESTRATOR EVOLVING

---

## 1. CURRENT SYSTEM STATE

### CORE PIPELINE STATUS: LOCKED

The core deterministic pipeline is **COMPLETE, FUNCTIONAL, and HARDENED**.

| Layer | Status | Description |
|-------|--------|-------------|
| Planner | LOCKED | Deterministic phrase matching, clean_input generation, tool_index aligned |
| Parser | LOCKED | Numeric + quoted string extraction |
| Resolver | LOCKED | Positional mapping only, clean_input consumption |
| Validation | LOCKED | Structural contract enforcement, FAIL-FAST |
| Execution | LOCKED | Output normalization PERMANENT, registry-driven |
| Tool Layer | LOCKED | Deterministic tools, contract-compliant |

**Determinism Guarantees:**
- Same input → same output (verified by harness)
- No randomness in any layer
- No side effects outside defined layers

### ORCHESTRATOR LAYER STATUS: FOUNDATION COMPLETE

The orchestrator layer (HAND) is **IMPLEMENTED** above system_entry.

| Component | Status |
|-----------|--------|
| orchestrator_runtime | IMPLEMENTED — workflow state machine, governance-controlled retry |
| agent_executor | IMPLEMENTED — LLM-based tool selection, retry guidance, context injection |
| agent_registry | IMPLEMENTED — in-memory agent registration |
| governance | IMPLEMENTED — decision authority for retry/fail/complete |
| intent_validator | IMPLEMENTED — structural validation, advisory semantic checks |
| persistence | PARTIAL — COMPLETED workflows only (active workflows NOT persisted) |
| tool_registry | IMPLEMENTED — orchestrator-layer tool registration |
| llm_registry | IMPLEMENTED — LLM provider registry (mock + Ollama) |
| task_classifier | IMPLEMENTED but NOT integrated — exists in create_workflow(), not in runtime flow |

**Orchestrator Characteristics:**
- Sequential multi-step workflows: IMPLEMENTED (limited)
- Retry behavior: IMPLEMENTED (governance-controlled)

- LLM-assisted planning: ACTIVE (non-deterministic)
- Parallel execution: NOT IMPLEMENTED

### INTEGRATION STATUS: COMPLETE

- ALL tool execution routes through system_entry
- Agent USE_TOOL: path routes through system_entry
- system_entry is the ONLY execution gateway

---

### TASK CLASSIFIER STATUS

* Location: system/orchestrator/task_classifier.py
* Status: IMPLEMENTED but NOT integrated into runtime flow
* Usage: Only used in create_workflow(), not in execute_from_input flow
* Authority: advisory only (no execution control)

Runtime flow uses plan_workflow() (LLM-based) instead of create_workflow() (classifier-based).

---

## 2. EXECUTION MODEL

### CORE PIPELINE (DETERMINISTIC, SINGLE-STEP)

```
Planner → Parser → Resolver → Validation → Execution
```

**Constraints:**
- NO multi-step logic
- NO chaining
- NO LLM-driven retries
- Deterministic execution only

### EXECUTION BOUNDARY

```
system_entry(input) → routes to Core Pipeline → returns Normalized Result
```

**Definition:**
- system_entry is the execution boundary between orchestrator and core pipeline
- It is NOT part of the core deterministic pipeline
- It is NOT the orchestrator layer itself
- It is the ONLY valid gateway for tool execution

### ORCHESTRATOR FLOW

```
Orchestrator Layer (runtime) → system_entry → Core Pipeline → Result
                                    ↑
                            Validation (advisory)
                            Governance (decision authority)
```

**Orchestrator Responsibilities:**
- Workflow execution control
- Agent coordination
- Step management
- Governance-controlled retry
- Context injection (last_result)

**Orchestrator Constraints:**
- MUST route ALL execution through system_entry
- MUST NOT access parser/resolver/validation/execution directly
- MUST NOT modify execution data
- MUST NOT introduce randomness into execution

---

## 3. RECENT FIXES (VERIFIED)

| # | Fix | Status | Code Validation |
|---|-----|--------|-----------------|
| 1 | Planner phrase-level priority matching | ✅ COMPLETE | confirmed |
| 2 | Resolver clean_input consumption | ✅ COMPLETE | confirmed |
| 3 | Execution output normalization | ✅ COMPLETE | confirmed |
| 4 | File tool contract restoration | ✅ COMPLETE | confirmed |
| 5 | Web search V2 (HTML scraping) | ✅ COMPLETE | confirmed |
| 6 | Validation empty string rejection | ✅ COMPLETE | confirmed |
| 7 | Float support enabled | ✅ COMPLETE | confirmed |
| 8 | Harness strict schema enforcement | ✅ COMPLETE | confirmed |
| 9 | Retry termination enforcement | ✅ COMPLETE | confirmed — max_retries enforced; workflow enters BLOCKED state when exceeded |
| 10 | Step input isolation | ✅ COMPLETE | confirmed — agent receives only step purpose, NOT full user input |
| 11 | Execution argument guard removed | ✅ COMPLETE | confirmed — Validation enforces exact argument count |
| 12 | Entry boundary sanitization removed | ✅ COMPLETE | confirmed — Resolver outputs contract-compliant structure |

---

## 4. CURRENT LIMITATIONS

### Structural Limitations

| Issue | Status | Impact |
|-------|--------|--------|
| Parser quoted string limitation | By design | No escaping/nesting support |
| Planner rule-based only | By design | No NLP, no inference |
| Float contract gap | Known | Float support exists in code but not formalized in contracts |
| Execution consistency variance | Known | Some tools may have inconsistent behavior patterns |

### Orchestrator Limitations

| Issue | Status | Impact |
|-------|--------|--------|
| LLM tool selection non-deterministic | Accepted | Selection may be incorrect |
| Semantic validation exists at orchestrator level | Advisory by design — no control flow influence | Does NOT reliably validate tool correctness or intent alignment; signals are advisory only |
| Multi-tool outputs rejected | Implemented | Only single USE_TOOL: allowed; multiple triggers rejection before execution |
| Response formatter non-authoritative | By design | LLM-based formatting may be incorrect |
| Memory system can learn incorrect tokens | Known | Valid tokens may be learned as "noise" |
| Persistence partial | Known | Only COMPLETED workflows persisted |
| Resume logic NOT implemented | Known | Cannot resume on restart |
| User control NOT implemented | Known | No pause/resume/cancel/override |

### Validation Limitations

Validation enforces:
- ✅ Contract structure
- ✅ Empty output rejection
- ✅ No-op detection

System does NOT validate:
- ❌ Tool correctness
- ❌ Tool necessity
- ❌ Semantic correctness
- ❌ Reasoning correctness

---

## SIGNAL LAYER — ADVISORY ONLY

The signal layer consists of:

* Planner (LLM-based workflow generation)
* Intent validator (advisory semantic signals)
* Tool-call observation (non-blocking logging)

Signal characteristics:

* Signals are advisory only
* Signals do NOT influence governance decisions
* Signals do NOT trigger control flow

Authority model:

* execution_result = sole decision driver
* governance = sole decision authority
* orchestrator executes decisions only

STATUS: ALIGNED

---

## SIGNAL LAYER RESOLUTION

Authority and flow realignment completed:

* Governance authority lock implemented — governance is sole decision authority
* execution_result is sole decision driver
* Signals (validator, planner, agent) do NOT independently influence control flow
* Mismatch detection exists but is stored as advisory metadata (_mismatch_advisory)

Retry behavior:

* clears previous execution state
* re-invokes agent using step["input"]
* represents a new attempt, not repetition

Final output MUST be gated by completion or governance decision.

---

## 5. CURRENT PHASE STATUS

### COMPLETED

- Core pipeline: LOCKED and HARDENED
- Orchestrator foundation: IMPLEMENTED
- Integration with core pipeline: COMPLETE
- Harness: COMPLETE for core pipeline + orchestrator external contract validation
- Tool contract alignment: COMPLETE
- Failure propagation verification: COMPLETE
- All steps now produce execution_result, including non-tool outputs via execution enforcement.
- Validation failures produce execution_result with failure status and reason.

### IN PROGRESS

- Documentation realignment
- Orchestrator layer stabilization
- Validation layer enhancement (signal correctness and alignment)

### PENDING

- Semantic validation (LLM-assisted, advisory only — implemented)
- Orchestrator harness coverage
- Resume logic implementation
- User control features
- Parallel execution (future consideration)

### BLOCKERS

- Contract alignment gaps (float definition) — minor
- Documentation realignment — in progress

---

## SYSTEM MODES

### SAFE MODE
- No LLM_MODEL set
- Passthrough behavior
- Fully deterministic

### INTELLIGENT MODE
- LLM_MODEL set
- LLM active for planning
- Structured output validated
- Invalid output → fallback to deterministic planner
- Non-deterministic tool selection expected

---

## FINAL OUTPUT RESOLUTION

Final output is determined strictly from execution_result:

- If execution_result exists:
  → final_output = execution_result
- Else:
  → final_output = None

Governance may control flow (retry/failure), but does NOT determine output content.

**Constraints:**
- NO synthesis
- NO merging
- NO fallback to step["output"]
- Output MUST be contract-compliant (status + result/reason)
- NO trace field in final output
- NO steps field in final output

---

## HARDENING STATUS

- Core harness system: IMPLEMENTED
- Strict schema enforcement: ACTIVE
- Determinism validation: ACTIVE
- Contract structure enforced: status + result/reason ONLY
- NO trace dependency in validation
- NO tool field validation (uses name field)

---

## 7. VALIDATION SYSTEM ARCHITECTURE

### LAYER DEFINITIONS

| Layer | Entry Point | Contract Type | Location | Purpose |
|-------|-------------|---------------|----------|---------|
| **Harness** | `execute_from_input` | Layered (nested) | `tests/harness_cases/` | External contract validation |
| **Internal Tests** | `run_workflow` | Flat (direct) | `system/tests/` | Behavior validation |

### BOUNDARIES

**Harness MUST NOT:**
- Test retry logic directly
- Access `run_workflow` 
- Validate internal step states
- Test lifecycle transitions

**Internal Tests MUST NOT:**
- Test `execute_from_input`
- Validate layered contract
- Duplicate harness validation
- Use natural language input

### CONTRACT SEPARATION

**run_workflow (Flat):**
```python
{"status": "success", "result": <value>}
{"status": "failure", "reason": <string>}
```

**execute_from_input (Layered):**
```python
{"status": "success", "result": {"status": "success", "result": <value>}}
{"status": "failure", "reason": <string>}
```

### AUTHORITY MODEL

- **Harness = CONTRACT AUTHORITY**: Validates what the system RETURNS
- **Internal Tests = BEHAVIOR AUTHORITY**: Validates how the system BEHAVES

See: `Project Docs/VALIDATION_ARCHITECTURE.txt` for full specification.

---

## END OF FILE
