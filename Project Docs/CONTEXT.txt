# AI LAB — CONTEXT (SOURCE OF TRUTH)

---

## PURPOSE

This document reflects the TRUE current system state.

This document MUST match:

* actual code behavior
* harness results

NOT intended architecture.

---

## 🚨 CURRENT SYSTEM STATE

STATUS: SYSTEM HARDENING → IN PROGRESS
SYSTEM → FUNCTIONAL but REQUIRES ALIGNMENT

System is FUNCTIONAL but RECENTLY HARDENED and REQUIRES ALIGNMENT

Multiple fixes recently applied:
* Planner phrase-level priority matching
* Resolver command phrase stripping
* File tool contract restoration
* Web search rewrite (HTML parsing)
* Validation empty string rejection
* Float support enabled

---

## SYSTEM STATUS

- Planner Layer → IMPLEMENTED AND VERIFIED
- Parser Layer → IMPLEMENTED AND VERIFIED
- Resolver Layer → IMPLEMENTED AND VERIFIED
- Entry Layer → IMPLEMENTED AND VERIFIED
- Validation Layer → IMPLEMENTED AND VERIFIED
- Execution Layer → IMPLEMENTED AND VERIFIED
- Tool Layer → IMPLEMENTED AND VERIFIED
- System Pipeline → FUNCTIONAL

---

## CURRENT RISKS

* Harness incomplete (contract validation gap)
* Float support not formally defined in contracts
* Resolver behavior expanded (phrase stripping)
* Tool consistency historically unstable
* Documentation requires alignment with implementation

---

## RECENT FIXES

1. **Planner Phrase-Level Priority Matching** — Longest phrase match implemented
2. **Resolver Command Phrase Stripping** — Removes command phrases before argument construction
3. **File Tool Contract Restoration** — Structured dict return enforced
4. **Web Search V2 Implementation** — HTML scraping replaces API
5. **Validation Empty String Rejection** — Empty/whitespace arguments rejected
6. **Float Support Enabled** — Parser + execution support float (contract update pending)

---

## SYSTEM PHASE

SYSTEM HARDENING → IN PROGRESS

Contracts require realignment. Harness has known limitations.

---

## VERIFIED PIPELINE

Planner → Parser → Resolver → Entry → Validation → Execution

---

## VERIFIED BEHAVIOR

- Validation runs before execution
- Fail-fast enforced at Entry layer
- Execution only runs on validation success
- PREVIOUS_RESULT resolved at Resolver layer (deterministic)
- Tool identity defined by tool_index ONLY
- Registry_builder constructs BOTH registries from tool_index
- Execution output uses `trace` instead of `steps` (observability field)
- Output normalization strips structural execution details
- Input normalization exists INSIDE planner (not pre-planner layer)

---

## COMPONENT STATUS

- Planner → IMPLEMENTED (deterministic, multi-step, tool_index aligned, phrase-level priority)
- Parser → IMPLEMENTED (numeric + float extraction)
- Resolver → IMPLEMENTED (PREVIOUS_RESULT injection, deterministic, phrase stripping)

---

## LLM STATUS

- LLM Adapter → IMPLEMENTED AND LOCKED
- Ollama support → ACTIVE (local, no API key required)
- LLM Entry → CONNECTED TO ADAPTER

---

## LLM BEHAVIOR MODES

The system operates in TWO modes:

MODE 1 — SAFE MODE (NO ENV):
- LLM_MODEL not set
- Adapter returns failure
- llm_entry returns input_text (passthrough)
- System remains fully deterministic

MODE 2 — INTELLIGENT MODE (ENV ENABLED):
- LLM_MODEL is set
- LLM adapter executes LLM call
- Output parsed and validated
- Returns structured plan (list of steps)
- Invalid output → guaranteed fallback to deterministic planner

---

## SYSTEM STATE — FUNCTIONAL

- Deterministic pipeline enforced
- All layers aligned with architecture law
- Router strictly routes (no interpretation)
- Planner fully authoritative
- Parser non-blocking
- Validation correctly scoped
- Execution reachable
- Final response contract enforced

---

## INVARIANT PROTECTION

- System behavior protected via invariant tests
- Any architectural drift triggers test failure
- Determinism guaranteed across runs

---

### LLM ENTRY VALIDATION — COMPLETE

- Strict structured validation enforced at llm_entry
- Invalid LLM outputs rejected deterministically
- Passthrough fallback guaranteed

---

### DUAL-MODE PIPELINE — VERIFIED

SAFE MODE:
- deterministic passthrough confirmed

INTELLIGENT MODE:
- structured output validated
- invalid outputs rejected
- adapter failure handled safely

---

### FULL PIPELINE VALIDATION — IN PROGRESS

- End-to-end tests added
- All dual-mode scenarios verified:
  - safe mode
  - intelligent mode (valid)
  - intelligent mode (invalid fallback)
  - adapter failure

- Harness results:
  - Tool tests passing (0 failures)
  - Contract validation requires hardening

---

## TOOL LIMITATIONS

- parser numeric + float extraction
- no string extraction from input text
- planner rule-based only
- deterministic input dependency
- run_python disabled (production=false)

---

## SYSTEM FUNCTIONAL STATE

- pipeline complete
- system_entry enforced
- contracts being realigned
- harness primary validation tool (with known limitations)

NEXT PHASE:

- harness hardening (contract validation)
- controlled planner expansion

---

## IMPORTANT RULE

If anything is uncertain:

→ mark as: NOT VERIFIED

---

END OF FILE
