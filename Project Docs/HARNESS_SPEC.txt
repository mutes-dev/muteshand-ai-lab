# AI LAB — HARNESS SPECIFICATION

---

## PURPOSE

Defines how system correctness is verified.

Harness is PRIMARY VALIDATION TOOL and COMPLETE for core pipeline validation.

Orchestrator manually validated. Harness integration pending.

---

# 🧪 TEST PRINCIPLES

Harness tests:

* behavior
* invariants
* contracts

NOT implementation details

---

# 🧱 LAYERED HARNESS MODEL

The harness must support testing at different system layers.

Harness MUST NOT assume full system availability.

Harness validates the COMPLETE core pipeline:

system_entry → validation → execution

Core Pipeline Properties:
- single-step only
- deterministic
- no planning
- no intelligence
- no LLM involvement

NOTE: system_entry is the SOLE execution gateway. It is the entry boundary to the core deterministic pipeline.

---

## ⚠ ORCHESTRATOR HARNESS NOTE

The harness validates via system_entry and execute_from_input.

Orchestrator external contract is covered; behavior validation is handled via internal tests.

Rules:
- Harness MUST use system_entry for core pipeline tests
- Harness MUST use execute_from_input for orchestrator external contract tests
- Orchestrator MUST NOT bypass the harness-tested pipeline
- Orchestrator behavior (retry, lifecycle) is validated via internal tests in system/tests/

Harness MUST validate the system ONLY through designated entry points.

This ensures:
- no execution bypass
- full pipeline validation
- consistency with system architecture

---

# 🔍 REQUIRED TEST TYPES

---

## 0. SYSTEM_ENTRY TEST (PRIMARY)

Harness MUST use:

* system_entry for core pipeline tests
* execute_from_input for orchestrator external contract tests

All tests execute via designated entry point:

```python
result = system_entry(input_text)  # core pipeline
result = execute_from_input(input_text)  # orchestrator external
```

NO direct layer testing.
NO bypassing designated entry points.

---

## 1. DETERMINISM TEST

Same input → same output

Run:

* 3 times minimum

FAIL if:

* outputs differ

---

## 2. CONTRACT TEST

Verify:

* input format accepted
* output format matches contract

FAIL if:

* missing fields
* incorrect structure

---

## 3. PARSER CONTRACT TEST

Verify:

* orchestrator-provided input is correctly parsed
* numeric arguments extracted deterministically (including floats)
* failure passthrough preserved

FAIL if:

* parser modifies failure object
* parser introduces non-numeric args
* parser performs inference

---

## 4. RESOLVER CONTRACT TEST

Verify:

* performs positional mapping only
* does NOT filter tokens
* does NOT type convert
* does NOT infer values
* does NOT reorder arguments
* does NOT access registry
* does NOT access execution results

FAIL if:

* resolver performs inference
* resolver filters tokens
* resolver accesses external data

---

## 5. EXECUTION TEST

Verify:

* correct result for known inputs

Example:

* add 2 and 3 → 5

---

## 6. FAILURE TEST

Verify:

* invalid input produces failure
* system does NOT attempt execution

---

## 7. FAILURE CONTRACT VALIDATION

Verify that ALL failure outputs strictly match the execution contract.

Requirements:

- Output MUST include:
  - "status"
  - "reason"

- "status" MUST equal "failure"
- "reason" MUST be a non-empty string

FAIL if:

- "reason" field is missing
- "reason" is not a string
- "reason" is empty
- "status" is not "failure"

---

## 8. TOOL RETURN SCHEMA VALIDATION (REQUIRED)

Verify ALL tool outputs follow contract:

SUCCESS:
```
{"status": "success", "result": <data>}
```

FAILURE:
```
{"status": "failure", "reason": <string>}
```

FAIL if tool returns raw string instead of structured dict.

---

# HARD RULES

* Harness MUST use real execution
* NO simulated outputs
* NO assumptions
* ALL results must include raw outputs

---

# FULL PIPELINE VALIDATION

Harness MUST validate:

system_entry → validation → execution

Core Pipeline (deterministic, single-step):
- Parser (input → tokens)
- Resolver (tokens → validated call)
- Validation (structural checks)
- Execution (tool invocation)

NOTE: system_entry is the SOLE execution gateway. It routes orchestrator requests to the core pipeline.

Invalid input NEVER reaches Execution.
Resolver output MUST match Entry contract exactly.

---

# ORCHESTRATOR LAYER SEPARATION

Orchestrator Layer (NON-CORE):

Components:
- planner (LLM-based workflow generation)
- multi-step workflow coordination
- governance (decision authority)
- step scheduling and lifecycle management

Rules:
- planner exists HERE (NOT in core)
- multi-step workflows handled HERE
- ALL execution routed via system_entry
- NO execution bypass allowed

Execution Flow:

User → Orchestrator → system_entry → Core → result

Harness Scope:
- Harness validates system_entry (core execution)
- Harness validates execute_from_input (orchestrator external contract)
- Harness does NOT validate orchestrator internals (planner, governance)

---

# EXECUTION RULE

ALL execution MUST go through system_entry.

Harness MUST validate:
- no tool execution occurs outside system_entry
- execute_from_input routes THROUGH system_entry (does NOT bypass)
- direct tool calls are PROHIBITED

Violation = architectural bypass (P0)

---

# EXECUTION MODEL

Harness MUST:

- execute tests using real system
- support subprocess execution
- enforce fail-fast at test runner level

---

# REGISTRY ENFORCEMENT

Harness MUST ensure:

- validation receives validation_registry
- execution receives execution_registry
- entry receives BOTH

FAIL if any mismatch occurs

---

# LLM MODE AWARENESS

The harness operates in SAFE MODE by default.

SAFE MODE:
- No LLM dependency
- deterministic behavior required

INTELLIGENT MODE (OPTIONAL):

- Requires explicit environment configuration
- Determinism is NOT required at LLM output level
- Validation layer MUST enforce contract correctness

Harness MUST:

- validate deterministic behavior in SAFE MODE
- validate contract compliance in INTELLIGENT MODE
- NEVER depend on LLM correctness

---

# STRICT SCHEMA VALIDATION (HARD GATE)

Harness enforces STRICT contract validation:

```python
def validate_strict_contract(result):
    # MUST be dict
    if not isinstance(result, dict):
        return False, "result is not a dict"
    
    # MUST have status field
    if "status" not in result:
        return False, "missing status field"
    
    # Success: MUST have result field, MUST NOT have reason
    if result["status"] == "success":
        if "result" not in result:
            return False, "success missing result"
        if "reason" in result:
            return False, "success has reason field"
        if len(result) != 2:
            return False, "success has extra fields"
        return True, None
    
    # Failure: MUST have reason field, MUST NOT have result
    if result["status"] == "failure":
        if "reason" not in result:
            return False, "failure missing reason"
        if "result" in result:
            return False, "failure has result field"
        if len(result) != 2:
            return False, "failure has extra fields"
        return True, None
    
    return False, "invalid status value"
```

Rules:
- EXACTLY 2 fields allowed: status + result (success) or status + reason (failure)
- NO extra fields allowed (no trace, no steps, no metadata)

---

## HARNESS CAPABILITIES

* STRICT schema enforcement implemented (hard gate)
* Contract structure validation: status + result/reason ONLY
* NO trace dependency in validation
* NO tool field validation (uses name field)
* Tool output normalization verified
* Determinism validation active
* Failure structure validation active
* Full contract validation
* Schema enforcement tests

---

## REMOVED (SUPERSEDED)

* ~~Subset matching~~ — REPLACED with strict schema validation
* ~~Tool trace validation~~ — REMOVED (trace not in contract)
* ~~Tool field validation~~ — REMOVED (name field is canonical)

---

## LAYERED CONTRACT VALIDATION

For orchestrator tests (execute_from_input), harness validates:

* Outer contract: {"status": "success", "result": <inner>} or {"status": "failure", "reason": <string>}
* Inner contract: {"status": "success", "result": <value>} or {"status": "failure", "reason": <string>}

Both layers MUST be structurally valid.

---

## FAILURE INJECTION RULE

Test failure scenarios using scoped monkeypatch ONLY:

* Patch specific functions for specific test files
* Automatic cleanup after test execution
* No global mutation of system behavior

Example: test_orchestrator_failure.py uses pytest fixture with monkeypatch.

---

## REQUIRED EXPANSION

Harness expansion is required for orchestrator-level validation.

---

# 🔒 SUCCESS CRITERIA

System is considered valid ONLY if:

* ALL tests pass
* determinism holds
* contract is respected
* full pipeline executes correctly
* dual registry model is enforced

Harness reflects actual system behavior and is used for final verification.

---

---

## ⚠️ LIMITATION — SEMANTIC VALIDATION

Harness validates:

✔ structure (contract fields, types)
✔ determinism (same input → same output)
✔ contract compliance (strict schema enforcement)
✔ argument count and type correctness
✔ limited semantic validation (execution mismatch)

Harness does NOT validate:

❌ reasoning correctness (was the right answer produced?)
❌ tool correctness (was the right tool selected?)
❌ tool necessity (should a tool have been used at all?)
❌ output relevance (does formatted output match the question?)

These limitations are ACCEPTED for the current phase.
The harness validates STRUCTURAL correctness only.
Semantic validation partially implemented at orchestrator level (intent validator), not covered by harness.

---

## ✅ ENFORCED — AUTHORITY BOUNDARIES

Harness validates authority model:

- execution_result is sole truth — validator signals are advisory only
- governance is sole decision authority — no signal-based control
- validator may detect mismatch but CANNOT trigger retry
- control flow is exclusively: execution_result → governance → decision

Behavioral drift (validator-triggered retry) has been resolved.

## ✅ ENFORCED — ORCHESTRATOR AUTHORITY

Retry behavior is governed solely by governance based on execution_result.
Harness validates external contract only and does not validate internal control flow.

Harness validates:

* execution via system_entry
* output contract compliance
* authority boundaries (execution_result = truth, governance = authority)

Signal layer is advisory only:

* validator signals do NOT influence retry or escalation
* governance is sole decision authority
* validator provides metadata, not control

Harness does NOT validate signal content correctness (semantic validation is advisory by design).

---

## 🚧 FUTURE REQUIREMENT

Extend harness AFTER Validator Layer is implemented:

- Add semantic correctness tests
- Add tool selection correctness tests
- Add tool necessity enforcement tests
- Add multi-tool output rejection tests

Until Validator Layer exists, these tests CANNOT be reliably enforced.

---

## LIMITATION — RETRY EFFECTIVENESS

Harness does NOT validate:

- whether retry improves outcome
- whether agent corrects behavior

Reason:

Retry depends on non-deterministic agent output.

---

## LIMITATION — TOOL SELECTION

Harness does NOT validate:

- correctness of tool selection
- appropriateness of tool usage

Incorrect tool usage may still produce structurally valid outputs.

---

## ✅ PARALLEL EXECUTION NOTE

Parallel execution behavior is validated at orchestrator level via harness tests:

- parallel group formation
- dependency enforcement
- destructive step exclusion
- execution ordering guarantees

Harness validates observable outcomes, not internal scheduler implementation.

---

END OF FILE
