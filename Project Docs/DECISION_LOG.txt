# DECISION LOG

## 2026-05 — Phase 1 Completion

Decision:
Mark Phase 1A and Phase 1B as COMPLETE and VALIDATED

Reason:

* Conflict detection validated under real execution
* Parallel execution validated with full contract compliance
* Governance, state transitions, trace, and synchronization verified

Impact:

* System now supports safe concurrent execution
* Phase 2 can proceed

---

## 2026-05 — Harness Realignment Required

Decision:
Introduce Phase 1C — Harness Realignment

Reason:

* System behavior changed (parallel execution + scheduling)
* Harness must reflect updated execution model

Impact:

* Ensures contract validation remains accurate
* Prevents false failures due to concurrency

---

## 2026-05 — Phase 2B Background Execution (COMPLETE)

Decision:
Implement Background Execution Manager as wrapper layer above orchestrator_runtime

Reason:

- Required by ORCHESTRATOR_CONTRACT_V2
- System previously blocked on synchronous execution
- Needed concurrent workflow support

Implementation:

- BackgroundManager introduced (system/runtime/)
- Thread-based execution model
- Workflow registry with lifecycle tracking
- No changes to orchestrator_runtime or execution flow

Impact:

- Multiple workflows can run concurrently
- Non-blocking execution enabled
- Enables future phases (checkpointing, memory)

Status: COMPLETE + VALIDATED

---

## 2026-05 — Phase 2C Checkpointing (COMPLETE)

Decision:
Implement step-level checkpointing system

Reason:

- Required for workflow resume
- Required for long task stability
- Required before persistence layer

Implementation:

- checkpoint_manager created
- save after step terminal state
- atomic writes
- restore support

Impact:

- prevents duplicate execution
- enables crash recovery
- no execution model changes

Status: COMPLETE + VALIDATED

---

## 2026-05 — Phase 2D Project Memory Persistence (COMPLETE)

Decision:
Implement persistence of ACTIVE workflows with restore capability

Reason:

- Required for system restart recovery
- Required for durable workflow state
- Required for Phase 3 memory system

Implementation:

- persistence.py extended
- ACTIVE/BLOCKED/PAUSED workflows saved
- restore before execution loop
- ACTIVE → PENDING normalization
- cleanup after completion

Impact:

- workflows survive restart
- system becomes durable
- enables next phase (memory system)

Status: COMPLETE + VALIDATED

---

## 2026-05 — Phase 1A Conflict Detection (ARCHIVED)

Decision:
Mark Conflict Detection as IMPLEMENTED BUT UNTESTED

Reason:
- Implementation completed in orchestrator layer
- Windsurf failed to execute real tests (manual review only)
- No verified runtime validation exists

Implications:
- System cannot guarantee conflict safety
- Parallel execution must remain disabled
- Phase 1A not considered complete

Next Action:
- Return to testing once reliable execution method is available

Status: RESOLVED — Phase 1A now validated and complete

---

## 2026-05 — Phase 1C Harness Realignment (COMPLETE)

Decision:
Complete harness realignment and extend validation to full orchestrator contract coverage

Reason:

- Harness based on outdated architecture assumptions
- Required enforcement of:
  - system_entry execution boundary
  - authority model (execution_result vs governance vs signals)
  - orchestrator contracts (schema, state, parallel, conflict)

Implementation:

- HARNESS_SPEC aligned to V2 architecture
- Execution path integrity tests added
- Authority model enforcement tests added
- Orchestrator contract validation added:
  - Step schema
  - State transitions
  - Parallel execution
  - Conflict detection

All tests executed using real system (no simulation)

Impact:

- Harness prevents architectural drift
- Execution boundary is provable
- Governance authority is provable
- Orchestrator correctness is provable

Status: COMPLETE + VALIDATED

---

## 2026-05 — Phase 3 Partial Completion + Planner Constraint Identified

Decision:
Mark Phase 3A, 3B, 3C as COMPLETE.
Mark Phase 3D and 3E as PARTIAL.

Reason:

* Memory implemented (advisory only)
* Drift implemented (observational only)
* Notifications implemented (output only)
* Dependency enforcement implemented
* Execution validity enforcement implemented

Limitation:

* Planner does not encode dependencies
* Agent allows incomplete input execution

Impact:

* System is correct but constrained
* Chaining not functional

Resolution:

* Phase 3D completion required (planner) — RESOLVED
* Phase 3E completion required (agent)

---

## 2026-05 — Phase 3D Dependency Model Completion

Decision:

Mark Phase 3D as COMPLETE

Reason:

- LLM-based dependency resolver implemented
- Planner no longer injects dependency semantics
- Strict normalization layer added
- DAG enforcement guarantees valid execution graph

Impact:

- Dependency correctness achieved
- Branching workflows now supported
- System enforces valid execution graph independent of LLM errors

Status: COMPLETE + VALIDATED

---

## 2026-05 — Deterministic Dependency Resolver Migration

Decision:
Replace LLM-based dependency resolver with deterministic parser.

Reason:

- LLM resolver introduced non-determinism and instability
- Dependency correctness inconsistent across runs
- Performance overhead (second LLM call per workflow)
- Architectural violation (intelligence in execution-adjacent layer)

Implementation:

- Removed LLM dependency resolver completely
- Removed normalization layer
- Introduced deterministic regex-based parser
- Planner updated to emit explicit step_X references

Impact:

- Dependency resolution is now deterministic
- Execution behavior is predictable
- Performance improved (no second LLM call)
- Dependency correctness now depends solely on planner output

Tradeoff:

- Loss of semantic inference
- Planner must correctly encode dependencies

Status: COMPLETE + VALIDATED

---

## 2026-05 — Live Streaming & Observability Implementation

Decision:
Implement event-driven observability system with early workflow_id exposure

Reason:
- Required real-time workflow visibility
- Blocking API prevented streaming
- Needed contract-compliant observability

Implementation:
- event_bus (in-memory queue)
- event_emitter (non-blocking)
- thread → workflow_id registry
- streaming API endpoints
- frontend polling model

Impact:
- Real-time step visibility achieved
- No impact on execution
- Observability fully decoupled from control flow

Status: COMPLETE + VALIDATED

---

## 2026-05 — GUI Architecture Realignment Required

Decision:

Reclassify GUI as **architecturally misaligned**, not partially implemented

---

Reason:

* Control layer violates workflow-scoped model
* Resume functionality broken due to missing context
* Streaming partially violates contract (event structure)
* Multiple sources of truth in UI

---

Key Insight:

"Thin UI" ≠ "Context-free UI"

GUI MUST:

* remain non-authoritative
* BUT include execution context (workflow_id)

---

Impact:

* Phase 4 must be extended with Phase 4F (realignment)
* Backend logic is validated and correct
* GUI is primary failure point

---

## 2026-05 — Control Model Clarification

Decision:

Control MUST be workflow-scoped

---

Reason:

* Explicit in GUI_FUNCTIONALITY_CONTRACT_V1
* Required for multi-workflow support
* Required for correct pause/resume behavior

---

Impact:

* All control endpoints require workflow_id
* Global control model deprecated

---

## 2026-05 — Streaming Model Clarification

Decision:

Streaming MUST be event-complete and self-contained

---

Reason:

* GUI_ARCHITECTURE_V2 requires event-level context
* Enables multi-workflow streaming
* Enables future WebSocket upgrade

---

Impact:

* Events must include workflow_id
* COMPLETE event required
* Polling model transitional only

---

## 2026-05 — Resume Flow Fix (GUI Integration)

Decision:
Fix resume streaming trigger via ControlPanel prop alignment

Root Cause:
onResumeStreamStart not passed into ControlPanel

Fix:

* Added prop to ControlPanel
* Resume now triggers stream polling

Impact:

* Resume fully functional
* Streaming continues after resume
* UI state transitions correct

Status:
COMPLETE + VALIDATED

---

## 2026-05 — Execution Model (Sync/Async Hybrid)

Decision:
Maintain synchronous orchestrator wrapped in async API

Implementation:

* API uses run_in_executor
* Orchestrator remains synchronous
* Resume runs in background thread

Reason:

* Avoid full async refactor
* Preserve deterministic execution
* Maintain existing contracts

Tradeoff:

* No progressive updates during resume
* Polling instead of real streaming

Impact:

* UI jumps from PAUSED → SUCCESS
* Streaming is observational, not real-time

Status:
ACCEPTED (Future improvement candidate)

