# AI LAB — DECISION LOG (VERSIONED)

---

## PURPOSE

This document records ALL architectural and system decisions.

Each decision MUST have a status:

* ACTIVE → currently valid and enforced
* TEMPORARY → allowed deviation (must be removed)
* REVOKED → no longer valid (kept for history)

---

## DECISION — STRICT OUTPUT CONTRACT (REVISED)

STATUS: ACTIVE (REVISED FROM TRACE/STEPS DECISION)

RULE:

system_entry output contains ONLY contract fields:

- Success: `{"status": "success", "result": <value>}`
- Failure: `{"status": "failure", "reason": <string>}`

Implementation:

- system_entry._normalize_output() enforces strict contract
- NO `trace` field in final output (captured internally only)
- NO `steps` field in final output (internal execution detail only)
- Execution performs output normalization (PERMANENT behavior)

REASON:

- Strict contract compliance
- No observability data leaked to caller
- Deterministic output structure

PREVIOUS DECISION "Trace replaces steps" SUPERSEDED — both removed from final output.

---

## DECISION — NORMALIZATION DRIFT ACKNOWLEDGED

STATUS: ACTIVE

RULE:

Input normalization currently located INSIDE planner.
Pre-planner normalization layer does NOT exist.

REASON:

- Identified via audit (evidence-based)
- Architectural documentation claimed pre-planner layer
- Code reality: normalization functions inside deterministic_planner.py
- Scheduled for architectural realignment

---

## DECISION — SEMANTIC MAPPING LOCATION

STATUS: ACTIVE

RULE:

Semantic keyword mapping (square→square_number, cube→cube_number) exists in normalize_input() function inside planner.

REASON:

- Functionally works but violates layer separation
- Should be planner responsibility, not normalization
- Documented for future architectural cleanup

---

# 🧠 ACTIVE DECISIONS (CURRENT TRUTH)

---

## DECISION — EXECUTION FAILURE PROPAGATION

STATUS: ACTIVE

RULE:

Execution layer MUST propagate tool-level failure objects directly.

Implementation:

- Check if tool output is dict with status="failure"
- If true: return failure object immediately
- If false: wrap in success response as normal

Rationale:

- prevents nested failure structures
- enforces consistent failure contract
- aligns execution with system-wide error handling

---

## DECISION — SYSTEM INVARIANT LOCK

STATUS: ACTIVE

RULES:

- All core behaviors protected by invariant tests
- No layer may change responsibility
- Router must not interpret input
- Planner is sole authority on intent
- Validation must not block valid flows
- system_entry enforces final contract

REASON:

Prevents regression and ensures long-term system stability.

---

## DECISION — ROUTER SIMPLICITY RULE

STATUS: ACTIVE

RULE:

- ALL string input → planner
- NO interpretation in router

REASON:

Prevents pipeline corruption and preserves planner authority.

---

## DECISION — TOOL INDEX AS SINGLE SOURCE OF TRUTH

tool_index defines tool identity via tool["name"].

Registry_builder is the ONLY consumer of tool_index.
Both validation_registry and execution_registry are derived from tool_index.

Status: ACTIVE
Reason: Prevents aliasing and ensures consistency

---

## DECISION — PLANNER ALIGNMENT TO TOOL INDEX

Planner MUST align to tool_index (no aliasing).

Planner:
* proposes tool candidates via keyword matching
* validates against VALID_TOOL_IDS from tool_index
* returns "unknown" for non-existent tools

Status: ACTIVE
Reason: Planner does NOT define independent tool truth

---

## DECISION — RESOLVER AS DETERMINISTIC ARGUMENT LAYER

Resolver introduced as deterministic argument preparation layer.

Purpose:
* perform positional mapping of tokens to arguments
* prepare execution-ready arguments

Rules:
* NO inference
* NO filtering
* NO type conversion
* NO registry access
* NO execution result access
* deterministic behavior only

Status: ACTIVE
Reason: Separates argument preparation from execution

---

## DECISION — ARGUMENT OWNERSHIP

Arguments belong ONLY to the Argument Resolver.

* Planner MUST NOT generate args
* Parser MUST NOT generate args

Status: ACTIVE
Reason: Enforces deterministic architecture

---

## DECISION — PARSER SIMPLIFICATION

Parser is a deterministic translation layer.

* Translates planner output → execution format
* Extracts numeric arguments and quoted strings
* NO inference
* NO semantic interpretation
* DOES NOT own arguments

Status: ACTIVE

---

## DECISION — FAILURE MODEL

System uses explicit failure objects.

* No silent failure
* No exception leakage

Status: ACTIVE
Reason: deterministic error handling

---

## DECISION — DUPLICATE DETECTION CONTROL

Duplicate planner output:

* MUST NOT proceed to execution
* MUST trigger retry flow

Status: ACTIVE
Reason: prevent undefined execution state

---

## DECISION — INPUT NORMALIZATION LOCATION

Input normalization occurs inside the planner (orchestrator-controlled).

* Preserves quoted strings
* Removes noise tokens
* Standardizes casing

Status: ACTIVE
Reason: Deterministic planner performs normalization before plan generation

---

## DECISION — PHRASE STRIPPING CONSOLIDATION

Phrase stripping MOVED from Resolver → Planner.

Planner:
* performs phrase matching
* generates clean_input (input text minus matched phrase)

Resolver:
* consumes clean_input from planner
* NO phrase stripping logic
* NO TOOL_PHRASES dictionary

Status: ACTIVE
Reason: Eliminates duplication, single source of truth for phrase handling

---

## DECISION — NORMALIZATION MODEL

Normalization uses:

* structural matching
* strict positional rules

Status: ACTIVE
Reason: maintain deterministic behavior

---

## DECISION — REGEX USAGE CONSTRAINT

Regex usage is FORBIDDEN in:

- input normalization
- argument parsing
- semantic inference
- planner reasoning

Regex usage is ALLOWED ONLY in:

- deterministic semantic mapping

CONSTRAINTS:

- patterns must be explicit and fixed
- one pattern maps to exactly one tool
- patterns must not overlap
- no ambiguity allowed
- tool must exist in registry before routing
- no argument extraction allowed at planner level

Purpose:

Enable controlled semantic access without introducing drift or inference.

Status: ACTIVE
Reason: controlled determinism vs blanket prohibition

---

## DECISION — EXECUTION EMBEDDED IN MANAGER

Execution remains inside manager (transitional).

Status: REVOKED

Reason:
Execution is being extracted into isolated execution layer (/system/execution).
Manager is now considered legacy and transitional only.

---

## DECISION — REMOVE PLANNER-LEVEL VALIDATION

Planner MUST NOT call validation.

Status: ACTIVE (FINAL)
Reason:

* Planner does not own arguments
* Prevents pre-pipeline failure
* Restores correct pipeline order

---

## DECISION — LLM ADAPTER INTEGRATION

LLM Adapter introduced as optional intelligence layer.

Rules:

- Adapter is internal to llm_entry
- Adapter output MUST be validated
- Adapter is NOT trusted
- Adapter failure MUST NOT break system

LLM output is NON-AUTHORITATIVE.

- may guide reasoning and formatting
- MUST NOT override execution truth
- MUST be bounded by system design

Status: ACTIVE
Reason: Enable controlled intelligence without breaking determinism

---

## DECISION — SYNTHESIS RESPONSIBILITY

Final output is produced by Orchestrator (HAND).

- execution provides grounded data
- LLM interprets and presents results
- no hardcoded selection rules allowed

STATUS: REVOKED

REASON:

Synthesis layer has been removed from system architecture.

Final output is produced by agent execution and selected by orchestrator.
No separate synthesis layer exists.

---

## DECISION — TOOL SELECTION NON-DETERMINISM

- tool selection is LLM-driven
- incorrect tool selection possible
- retry logic is not reliable

---

## DECISION — LLM ENTRY FALLBACK AUTHORITY

llm_entry defines observable behavior for LLM usage.

Rules:

- Adapter failure → return input_text
- Invalid output → return input_text
- Only valid structured output passes through

Status: ACTIVE
Reason: Preserve deterministic system behavior

---

## DECISION — DUAL-MODE SYSTEM BEHAVIOR

System operates in two modes:

SAFE MODE:
- No LLM_MODEL
- passthrough behavior

INTELLIGENT MODE:
- LLM active
- structured output

Status: ACTIVE
Reason: Support optional intelligence without breaking system guarantees

---

# ⚠️ TEMPORARY DECISIONS (CONTROLLED DEVIATIONS)

---

## DECISION — RUNTIME CHAIN RESOLUTION

Chain Resolver executes inside the execution loop.

- PREVIOUS_RESULT is a runtime dependency
- Resolution requires actual execution results
- Therefore cannot occur in structural phase

Status: ACTIVE

Reason:
Runtime dependency resolution requires access to execution results.
Moving this earlier introduces invalid data and breaks determinism.

Conclusion:
Chain resolution is a runtime concern and MUST remain inside execution.

---

## TEMP DECISION — MODULE BOUNDARY RELAXATION (RECOVERY)

During debugging:

* Changes were made inside logically locked modules

Violation:

* Breaks module isolation discipline

Reason:

* Required to identify hidden system failures

Status: TEMPORARY
Priority: HIGH

Required Fix:

* Re-lock all modules after pipeline correction

---

## TEMP DECISION — EXECUTION LOGGING

Execution layer logs:

* argument mismatches
* debug traces

Violation:

* minor purity violation

Status: TEMPORARY
Reason: debugging visibility

Must be removed: Phase 2+

---

## DECISION — RESOLVER POSITIONAL MAPPING MODEL

Resolver performs positional mapping only.

Content:
* Resolver performs positional mapping only
* No filtering of tokens
* No type enforcement
* Tokens mapped positionally to arguments

Status: ACTIVE
Reason: Aligns with actual system behavior

---

# ❌ REVOKED DECISIONS (HISTORICAL — DO NOT USE)

---

## REVOKED — PHASE 1 COMPLETION

Original Claim:

Phase 1 is complete and system stabilized.

Status: REVOKED

Reason:

* Hidden validation existed
* Planner parsing bug existed
* Chain resolution misplaced
* Modules modified after lock

Conclusion:

Phase 1 is NOT complete.

---

## REVOKED — PLANNER VALIDATION COUPLING

Original Behavior:

Planner calls validation during plan generation.

Status: REVOKED

Reason:

* Violates layer isolation
* Causes pre-pipeline failure

---

## REVOKED — PLANNER ARGUMENT OVERRIDE

Original Behavior:

Planner injected:

* args
* PREVIOUS_RESULT

Status: REVOKED

Reason:

* Violates Argument Ownership

---

## REVOKED — DUPLICATE CHAIN LOGIC IN MANAGER

Original Behavior:

Multiple chain resolution paths existed.

Status: REVOKED

Reason:

* Chain Resolver must be single source of truth

---

# 🧭 CURRENT SYSTEM STATE

---

## DECISION — ENTRY LAYER

Entry is pure orchestration layer.

Rules:

- No logic allowed inside Entry
- Fail-fast enforced at Entry boundary
- Entry receives structured plan (NOT raw input)
- Entry calls Validation → Execution only

Status: ACTIVE

---

## DECISION — SEMANTIC MAPPING LAYER

Planner includes deterministic semantic pattern matching:

Execution Order:
1. Normalize input
2. Direct tool match
3. Semantic pattern match
4. Else → structured failure

Pattern Rules:
- Explicit regex patterns only
- One pattern maps to exactly one tool
- Patterns must not overlap
- Tool must exist in registry
- No argument extraction

Status: ACTIVE
Reason: Enable direct tool accessibility without unsafe fallbacks

---

## DECISION — FALLBACK REMOVAL

Unsafe fallback behavior REMOVED.

Previous behavior:
- Any input could be treated as tool name
- No validation of tool existence

Current behavior:
- Only valid tool names from registry allowed
- Unknown inputs return structured failure

Status: ACTIVE
Reason: Prevent execution of arbitrary/invalid tools

---

## DECISION — DETERMINISTIC ROUTING

Planner routing is now FULLY deterministic for known inputs:

Direct match → immediate return
Semantic match → immediate return
Unknown input → immediate failure

Only math operations use LLM (by design).

Status: ACTIVE
Reason: Reduce LLM dependency for common operations

---

## DECISION — SEMANTIC ROUTING WITHOUT ARGUMENT EXTRACTION

- Planner routes semantic inputs only
- Argument extraction remains resolver responsibility
- No inference allowed at planner level
- input_text preserved unchanged

Status: ACTIVE
Reason: Maintain layer separation, no planner argument generation

---

## DECISION — VALIDATION FAILURE IS ACCEPTABLE FOR SEMANTIC INPUTS

- Missing arguments cause validation failure
- System must NOT guess values
- Explicit failure is correct behavior
- Resolver enhancement will address in Phase 3

Status: ACTIVE
Reason: Prevent silent inference, enforce explicit argument handling

---

# ❌ REVOKED DECISIONS (HISTORICAL — DO NOT USE)

---

## REVOKED — PHASE 1 COMPLETION

Original Claim:

Phase 1 is complete and system stabilized.

Status: REVOKED

Reason:

* Hidden validation existed
* Planner parsing bug existed
* Chain resolution misplaced
* Modules modified after lock

Conclusion:

Phase 1 is NOT complete.

---

## REVOKED — PLANNER VALIDATION COUPLING

Original Behavior:

Planner calls validation during plan generation.

Status: REVOKED

Reason:

* Violates layer isolation
* Causes pre-pipeline failure

---

## REVOKED — PLANNER ARGUMENT OVERRIDE

Original Behavior:

Planner injected:

* args
* PREVIOUS_RESULT

Status: REVOKED

Reason:

* Violates Argument Ownership

---

## REVOKED — DUPLICATE CHAIN LOGIC IN MANAGER

Original Behavior:

Multiple chain resolution paths existed.

Status: REVOKED

Reason:

* Chain Resolver must be single source of truth

---

---

## DECISION — INPUT NORMALIZER INTRODUCED

Input Normalizer positioned before planner.

Purpose:
- Remove noise tokens
- Normalize casing
- Improve usability

Constraints:
- Structure-safe only
- No argument creation
- No reordering

Status: ACTIVE

---

## DECISION — DETERMINISTIC TOOL MAPPING

TOOL_PHRASES introduced in planner.

Behavior:
- Explicit phrase → tool mapping
- Longest match first
- No LLM dependency for routing

Status: ACTIVE

---

## DECISION — CONNECTOR-ONLY SPLITTING

Chain detection uses ONLY explicit connectors:
- " then "
- " and then "
- " then and "

"and" removed as split trigger.

Status: ACTIVE

---

## DECISION — ARGUMENT SHAPE FILTERING

Argument Resolver uses allow-list filtering.

Status: REVOKED

Reason:
Resolver performs positional mapping only. No filtering. No allow-list.

---

# 🔥 FINAL PRINCIPLE

Decisions are not truth.

Architecture is truth.

If a decision conflicts with architecture:

→ decision must be REVOKED

---

## DECISION — FAIL-FAST VALIDATION (MANAGER)

Manager enforces validation BEFORE execution.

Rules:

- Invalid planner_output → return "invalid_input"
- Empty planner_output → return "invalid_input"
- Malformed steps → return "invalid_input"
- Execution MUST NOT start if validation fails

Status: ACTIVE

Reason:
Prevents execution leakage and restores architectural boundary between validation and execution.

---

## DECISION — PREVIOUS_RESULT FAILURE MOVED TO EXECUTION

PREVIOUS_RESULT validation removed from validation layer.

Status: REVOKED

Reason:
System no longer supports PREVIOUS_RESULT. Single-step execution only.

---

## DECISION — CLEAN SYSTEM REBUILD

System is being rebuilt using isolated layered structure.

New structure:

* /core → logic layers
* /system → execution, entry, observability

Manager is deprecated and used only for extraction.

Status: ACTIVE

Reason:
Current system is deterministic but structurally compromised.
Rebuild required to enforce strict layer isolation.

---

## DECISION — CONTRACT-DRIVEN DEVELOPMENT

All new system components must be built using explicit contracts:

* execution contract
* trace contract
* validation contract

No implementation allowed without defined contract.

Status: ACTIVE

Reason:
Prevents drift and enforces deterministic behavior.

---

## DECISION — FLOAT SUPPORT ENABLED

Parser and execution now support float types.

Rules:

- "number" type in tool_index accepts both int and float
- Parser extracts float values (e.g., 3.14, -0.5)
- Validation accepts float where type = number
- Execution processes float without coercion

Status: ACTIVE

Reason:
Extend numeric capabilities while maintaining deterministic behavior.

Note: Contract update pending (formal float definition in SYSTEM_CONTRACTS.txt)

---

## DECISION — PLANNER PHRASE-LEVEL PRIORITY

Planner implements longest-phrase-first matching.

Behavior:

- TOOL_PHRASES defines explicit phrase → tool mappings
- Phrases sorted by length (longest first)
- Word boundary enforcement
- Deterministic priority for overlapping phrases

Status: ACTIVE

Reason:
Ensure deterministic routing when multiple phrases could match.

---

## DECISION — RESOLVER PHRASE STRIPPING

STATUS: REVOKED — MOVED TO PLANNER

Original Behavior (REVOKED):

- Resolver used TOOL_PHRASES mapping
- Resolver stripped matched phrase from input_text

Current Behavior (ACTIVE):

- Planner performs phrase matching
- Planner generates `clean_input` (input text minus matched phrase)
- Resolver consumes `clean_input` from step (NO phrase stripping)
- Resolver has NO TOOL_PHRASES dictionary

Reason:
Eliminates duplication, single source of truth for phrase handling in planner layer.

---

## DECISION — FILE TOOL CONTRACT RESTORATION

File tools (read_file, list_files) now return structured dicts.

Behavior:

- {"status": "success", "result": <content>}
- {"status": "failure", "reason": <error_code>}
- No raw string returns

Status: ACTIVE

Reason:
Restore system contract compliance. All tools must return structured outputs.

---

## DECISION — WEB SEARCH V2 IMPLEMENTATION

Web search tool uses DuckDuckGo HTML endpoint.

Behavior:

- Fetches real search results
- Parses HTML with BeautifulSoup
- Extracts titles, URLs, snippets
- Returns top 3 results in structured format

Status: ACTIVE

Reason:
Replace limited Instant Answer API with comprehensive search results.

---

## DECISION — VALIDATION EMPTY STRING REJECTION

Validation layer now rejects empty string arguments.

Behavior:

- Empty strings fail validation
- Whitespace-only strings fail validation
- Returns argument_type_mismatch or similar

Status: ACTIVE

Reason:
Prevent invalid tool calls with empty required arguments.

---

## DECISION — STRUCTURAL BOUNDARY CLARIFICATION (Violation 1.1 Resolution)

STATUS: ACTIVE

RULE:

* Resolver constructs final execution structure
* Validation verifies and enforces contract compliance
* Entry performs no structural modification

STRUCTURE:

Resolver outputs EXACTLY:

```json
[
  {"name": string, "args": [...]}
]
```

No additional fields allowed.

REASON:

* Prevents reintroduction of sanitization loops
* Ensures correct layer responsibility
* Aligns system behavior with architecture laws
* Eliminates redundant data transformation

IMPLEMENTATION:

* Entry sanitization loop REMOVED from system_entry.py
* Resolver confirmed as single construction point
* Validation unchanged (read-only verification)

REFERENCES:

* system/resolver/argument_resolver.py:140-143 (construction)
* system/entry/system_entry.py:97 (pass-through)
* system/observability/validator.py:59-61 (verification)

---

## DECISION — VALIDATION AUTHORITY GUARANTEE (Violation 1.2 Resolution)

STATUS: ACTIVE

RULE:

- Validation is sole authority for structural correctness
- Execution assumes validated input
- Execution MUST NOT validate arguments

STRUCTURAL SEPARATION:

| Layer | Responsibility |
|-------|----------------|
| Validation | Enforces argument count + types |
| Execution | Assumes valid input, executes only |

REASON:

- Prevents duplication of validation logic
- Ensures strict layer separation
- Prevents reintroduction of execution guards
- Validation already enforces exact argument count match

IMPLEMENTATION:

- Argument count guard REMOVED from executor.py
- Validation unchanged (already enforced exact match)
- Execution now purely runtime (no structural checks)

REFERENCES:

- system/observability/validator.py:82-83 (argument count enforcement)
- system/execution/executor.py:90-91 (execution without validation)

---

## DECISION — CLEAN_INPUT CLASSIFICATION

STATUS: ACTIVE

RULE:

- clean_input is a planner-generated structural field
- It is NOT input normalization
- It MUST NOT be moved to pre-planner normalization layer

STRUCTURAL DEFINITION:

| Attribute | Value |
|-----------|-------|
| Type | Structural transformation |
| Created | Planner (line 261) |
| Consumed | Resolver (line 130) |
| Contract | NOT part of execution/validation contract |

REASON:

- Prevents misclassification of planner logic
- Maintains correct separation between preprocessing and structural transformation
- Avoids incorrect architectural refactoring

REFERENCES:

- system/planner/deterministic_planner.py:261 (generation)
- system/resolver/argument_resolver.py:130 (consumption)

---

## DECISION — HARNESS VALIDATION SCOPE

Harness enforces strict structural contract validation for core pipeline.

Known Limitations:

- Subset matching may miss structural violations
- Tool return schemas not strictly enforced
- Contract violations may pass tests

Status: ACTIVE (P0 Blocker)

Required Action:

- Expand harness with strict schema validation
- Add contract enforcement tests
- Implement failure structure validation

---

---

## DECISION — ORCHESTRATOR LAYER INTRODUCED ABOVE CORE PIPELINE

STATUS: ACTIVE

RULE:

Orchestrator layer is implemented above system_entry.
It does NOT replace any core pipeline layer.
system_entry remains the execution authority.

Implementation:
- /system/orchestrator/ contains all orchestrator components
- orchestrator_runtime.py routes fallback steps through system_entry
- Agents invoke system_entry for fallback (unregistered agent) steps

REASON:

- Workflow control, retry logic, and agent management require a layer above the pipeline
- Core pipeline must remain untouched and deterministic

---

## DECISION — AGENT TOOL EXECUTION SANDBOXED (TEMPORARY DEVIATION)

STATUS: TEMPORARY

RULE:

Previously bypassed system_entry — now resolved. Tool execution is routed through system_entry.

Both USE_TOOL: input path (agent_executor.py:77) and LLM-generated USE_TOOL: path
(agent_executor.py:234) call system_entry directly.

REASON:

- Architectural compliance. system_entry must be the sole execution gateway.

---

---

## DECISION — LLM-DRIVEN TOOL SELECTION INTRODUCED

STATUS: ACTIVE

RULE:

The orchestrator layer uses LLM to select tools dynamically.
LLM output is checked for USE_TOOL: prefix and routed to system_entry.

IMPLICATIONS:

- Tool selection is non-deterministic (LLM-dependent)
- Retry logic partially corrects LLM errors
- No guarantee of correct tool selection
- core pipeline (system_entry) remains deterministic

REASON:

Enable natural language → tool execution without hard-coded routing rules.

---

## DECISION — SYSTEM_ENTRY REMAINS DETERMINISTIC GATEWAY

STATUS: ACTIVE

RULE:

Despite LLM layer above it, system_entry MUST remain the sole execution authority.
All tool calls from the orchestrator MUST route through system_entry.
system_entry MUST NOT be modified to accommodate LLM non-determinism.

REASON:

Core execution correctness depends on deterministic pipeline.
Intelligence layer (LLM) operates above it, not inside it.

---

## DECISION — NO FALLBACK LOGIC INSIDE TOOLS

STATUS: ACTIVE

RULE:

Tools MUST NOT contain fallback logic for invalid inputs.
Tools MUST fail explicitly and return structured failure objects.
All fallback and retry logic belongs to the orchestrator layer.

REASON:

Prevents silent incorrect execution inside tool implementations.
Enforces clean layer separation.

---

## DECISION — SEMANTIC VALIDATION STATE

STATUS: ACTIVE

Semantic validation:

- NOT implemented
- Validator is structural only
- No tool correctness or output correctness validation exists

---

## DECISION — FORMATTER IS NOT A TRUSTED SOURCE OF TRUTH

STATUS: ACTIVE

RULE:

The response formatter (_format_tool_output in agent_executor.py) uses LLM to
produce human-readable output from raw tool results.
This output is ADVISORY ONLY and may be incorrect.
The formatter MUST NOT influence execution logic or retry decisions.

REASON:

LLM is non-authoritative. Formatted output may be hallucinated or misleading.

---

## DECISION — AGENT USE_TOOL PATH NOW ROUTES THROUGH SYSTEM_ENTRY

STATUS: ACTIVE (RESOLVES PREVIOUS TEMPORARY DEVIATION)

RULE:

Agent tool execution (USE_TOOL: prefix in agent_executor.py) routes
directly through system_entry for both direct USE_TOOL: input and
LLM-generated USE_TOOL: output.

CURRENT STATE:

Agent tool execution routes through system_entry.
All previous bypass behavior is REVOKED.

References:
- agent_executor.py:77 (direct USE_TOOL: path → system_entry)
- agent_executor.py:210 (LLM-generated USE_TOOL: path → system_entry)

REASON:

Architectural compliance. system_entry must be the sole execution gateway.

---

## DECISION — EXECUTION MEMORY SYSTEM INTRODUCED

STATUS: ACTIVE

RULE:

An execution memory system (execution_patterns.json) learns noise tokens
from failure→success attempt pairs and applies them to clean future inputs.

Guards enforced:
- Numeric tokens MUST NOT be stored (memory_guard)
- Quoted string tokens MUST NOT be stored (semantic filter)
- Path-like tokens (containing "/") MUST NOT be stored (semantic filter)

REASON:

Prevents memory poisoning while enabling noise reduction across retries.

---

## DECISION — VALIDATOR ENFORCEMENT

STATUS: REVOKED

RULE:

If execution_result exists AND validator decision != "accept":
→ execution_result enforced for final_output

⚠️ DRIFT:
This statement is no longer true in the system.

governance.py:resolve_decision controls final output:
- execution_result returned if exists
- else None

No fallback to step["output"] allowed.

Validator provides advisory signals only.
Governance makes final output decisions.

REASON:

Validator authority was incorrectly documented.
Governance is the final output authority.

---

## DECISION — SYNTHESIS NON-AUTHORITY

STATUS: REVOKED

REASON:

Synthesis layer no longer exists.

---

## DECISION — PREVIOUS_RESULT FAILURE MOVED TO EXECUTION (REVOKED)

PREVIOUS_RESULT validation removed from validation layer.

STATUS: REVOKED

REASON:

System now supports multi-step workflows through orchestrator layer.

Core execution pipeline remains single-step deterministic.
Orchestrator enables controlled multi-step execution above the pipeline.

---

## DECISION — ORCHESTRATOR AUTHORITY MODEL

STATUS: ACTIVE

RULE:

Orchestrator (HAND) is responsible for:

- workflow control
- execution flow decisions
- coordination of agents and steps

Constraints:

- MUST operate within SYSTEM_CONTRACTS and ARCHITECTURE_LAW
- MUST NOT override execution truth
- MUST NOT bypass system_entry

REASON:

Defines orchestrator as control layer without violating deterministic execution model.

---

## DECISION — GOVERNANCE AUTHORITY

STATUS: ACTIVE

RULE:

Governance layer is the final decision authority for:

- execution continuation
- retry vs escalate decisions
- conflict resolution between outputs

Constraints:

- operates on signals from:
  - validator
  - runtime
  - agent outputs
- MUST NOT override execution truth

REASON:

Separates decision-making from validation and execution layers.

---

## DECISION — VALIDATOR ROLE (ADVISORY)

STATUS: ACTIVE

RULE:

Validator is advisory only.

- validates contract compliance
- performs bounded structural post-execution checks (empty output, no-op)
- may recommend retry or escalation

Constraints:

- MUST NOT determine execution outcome
- MUST NOT override execution results

REASON:

Prevents authority overlap and ensures clean separation of concerns.

---

## DECISION — FINAL OUTPUT MODEL

STATUS: ACTIVE

RULE:

Final output is determined by orchestrator selection.

- output MUST originate from a single execution path
- outputs MUST NOT be merged or synthesized
- multiple outputs require:
  - selection OR
  - resolution step OR
  - user escalation

REASON:

Ensures deterministic and traceable output without synthesis layer.

---

END OF FILE
