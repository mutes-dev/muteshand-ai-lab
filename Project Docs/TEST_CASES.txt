# AI LAB — TEST CASES (SYSTEM_ENTRY PRIMARY)

## IMPORTANT

These tests validate via system_entry ONLY.

system_entry is the SINGLE execution gateway.

---

## PURPOSE

Validate system behavior through system_entry.

Primary test format:

```python
TEST_CASES = [
    {
        "input": "add 2 and 3",
        "expected": {
            "status": "success",
            "result": 5
        }
    }
]
```

Execution tests (secondary) apply to:

/system/execution/executor.py

---

## 🚨 RULE

Tests MUST execute via:

```python
result = system_entry(input_text)
```

NO direct layer testing.
NO bypassing system_entry.

---

## TEST TYPES

### 1. SYSTEM_ENTRY TESTS (PRIMARY)

Input: natural language string
Output: system response dict

### 2. EXECUTION TESTS (SECONDARY)

Input: structured plan
Output: execution result

For isolated execution layer validation only.

---

# 🧪 EXECUTION TEST CASES (PHASE 1)

---

## TEST 1 — BASIC ADD

Input:

[
{ "name": "add_numbers", "args": [2, 3] }
]

Expected:

✔ status = success
✔ result = 5

---

## TEST 2 — MULTI-STEP REJECTION

Input:

[
{ "name": "add_numbers", "args": [2, 3] },
{ "name": "multiply_numbers", "args": [4, 5] }
]

Expected:

✔ status = failure
✔ reason = single_step_required

---

## TEST 3 — INVALID TOOL

Input:

[
{ "name": "unknown_tool", "args": [1, 2] }
]

Expected:

✔ status = failure

---

## TEST 5 — DETERMINISM

Run same input 3 times.

Expected:

✔ identical outputs

---

# 🔒 SUCCESS CRITERIA

Execution layer is valid ONLY if:

* all tests pass
* outputs match contract
* determinism holds

---

## 🚨 ORCHESTRATOR EXECUTION RULE

Agent tool execution (USE_TOOL) MUST route through system_entry.

Tests MUST verify:

* no execution bypasses validation layer
* system_entry is used for all tool execution paths
* Harness enforces strict structural contract validation for core pipeline.

---

---

## 🧪 SEMANTIC TESTS (FUTURE — NOT IMPLEMENTED)

These tests represent future behavior.  
Failures are expected and NOT defects.

---

### SEMANTIC TEST 1 — WRONG TOOL SELECTION

Input: `what is 5 minus 3`

Expected (future):
- Tool selected = subtract_numbers ✔
- If LLM selects add_numbers → REJECTED

Current behavior: no semantic check — wrong tool passes if types are valid

---

### SEMANTIC TEST 2 — TOOL VS NO-TOOL DECISION

Input: `what is the definition of recursion`

Expected (future):
- No tool used — LLM responds directly ✔
- If USE_TOOL: triggered → REJECTED

Current behavior: LLM may trigger USE_TOOL: — no enforcement

---

### SEMANTIC TEST 3 — LOGICAL CORRECTNESS VALIDATION

Input: `what is 10 + 5`

Expected (future):
- result = 15 ✔
- result ≠ 15 → REJECTED, retry

Current behavior: result accepted regardless of correctness

---

## ORCHESTRATOR TEST — NON-TOOL CHAINING FAILURE

Input:
"Generate a random number, then add 10"

Expected (current behavior):

- First step produces LLM output (non-tool)
- execution_result = None
- Second step fails OR produces incorrect result

Validation:

- System does NOT crash
- Failure is observable
- Behavior matches KNOWN_ISSUES

NOTE:

This test documents current limitation.

---

### SEMANTIC TEST 4 — MULTI-TOOL OUTPUT REJECTION

LLM output: `USE_TOOL: add_numbers 2 3\nUSE_TOOL: subtract_numbers 5 1`

Expected (future):
- REJECTED — only one tool call allowed per response

Current behavior: REJECTED (multiple_tool_calls_not_allowed)

---

### SEMANTIC TEST 5 — FORMATTER CORRECTNESS

Input: `what is 2 + 3`
Tool output: `5`
Formatter output: `The answer is 5.`

Expected (future):
- Formatter output validated against tool output ✔
- Hallucinated formatter output → fallback to raw result

Current behavior: formatter output used without validation

---

## ⚠️ ORCHESTRATOR TEST GAP

These tests validate core pipeline behavior only.

They do NOT validate:
- orchestrator workflows
- multi-step execution
- LLM-driven decisions
- governance behavior

Additional orchestrator-level tests are required.

---

## ORCHESTRATOR / GOVERNANCE TESTS

Must validate:

- Retry occurs ONLY when governance returns "retry"
- No retry occurs when governance returns "fail"
- Governance decisions are dynamically resolved (monkeypatch must affect runtime behavior)
- Validator may trigger retry despite correct execution_result (drift)

---

## VALIDATOR DRIFT TEST

Input: `what is 2 + 2`

execution_result: 4 (correct)
candidate_output: "The sum is 4." (correct)

Expected:
- Validator accepts (no retry)

Current behavior:
- Validator may return "retry" due to formatting mismatch
- execution_result is correct but retry triggered anyway

This is behavioral drift (NOT intended architecture).

---

END OF FILE
