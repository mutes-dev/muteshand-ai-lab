# AI LAB — TEST CASES (PHASE 1 — EXECUTION LAYER)

## IMPORTANT

These tests validate EXECUTION ONLY.

LLM behavior is NOT covered here.

LLM-related behavior is validated via:

- harness TEST_CASES (llm_entry)
- adapter-level testing

Execution MUST remain independent of LLM.

---

These tests validate EXECUTION ONLY.

Parser and planner are validated separately via harness.

---

## PURPOSE

Validate isolated execution engine behavior.

This test suite applies ONLY to:

/system/execution/executor.py

---

## 🚨 RULE

Tests MUST NOT depend on:

* planner
* parser
* resolver
* manager

Execution is tested in isolation.

---

# 🧪 TEST CASES

---

## TEST 1 — BASIC ADD

Input:

[
{ "tool": "add_numbers", "args": [2, 3] }
]

Expected:

✔ status = success
✔ result = 5

---

## TEST 2 — MULTI-STEP EXECUTION

Input:

[
{ "tool": "add_numbers", "args": [2, 3] },
{ "tool": "multiply_numbers", "args": ["PREVIOUS_RESULT", 4] }
]

Expected:

✔ status = success
✔ result = 20

---

## TEST 3 — PREVIOUS_RESULT FAILURE

Input:

[
{ "tool": "multiply_numbers", "args": ["PREVIOUS_RESULT", 4] }
]

Expected:

✔ status = failure
✔ reason = missing_previous_result

---

## TEST 4 — INVALID TOOL

Input:

[
{ "tool": "unknown_tool", "args": [1, 2] }
]

Expected:

✔ status = failure

---

## TEST 5 — DETERMINISM

Run same input 3 times.

Expected:

✔ identical outputs

---

# 🔒 SUCCESS CRITERIA

Execution layer is valid ONLY if:

* all tests pass
* outputs match contract
* determinism holds

---

END OF FILE
