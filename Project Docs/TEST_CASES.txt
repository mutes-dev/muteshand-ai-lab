# AI LAB — TEST CASES (SYSTEM_ENTRY PRIMARY)

## IMPORTANT

These tests validate via system_entry ONLY.

system_entry is the SINGLE execution gateway.

---

## PURPOSE

Validate system behavior through system_entry.

Primary test format:

```python
TEST_CASES = [
    {
        "input": "add 2 and 3",
        "expected": {
            "status": "success",
            "result": 5
        }
    }
]
```

Execution tests (secondary) apply to:

/system/execution/executor.py

---

## 🚨 RULE

Tests MUST execute via:

```python
result = system_entry(input_text)
```

NO direct layer testing.
NO bypassing system_entry.

---

## TEST TYPES

### 1. SYSTEM_ENTRY TESTS (PRIMARY)

Input: natural language string
Output: system response dict

### 2. EXECUTION TESTS (SECONDARY)

Input: structured plan
Output: execution result

For isolated execution layer validation only.

---

# 🧪 EXECUTION TEST CASES (PHASE 1)

---

## TEST 1 — BASIC ADD

Input:

[
{ "name": "add_numbers", "args": [2, 3] }
]

Expected:

✔ status = success
✔ result = 5

---

## TEST 2 — MULTI-STEP REJECTION

Input:

[
{ "name": "add_numbers", "args": [2, 3] },
{ "name": "multiply_numbers", "args": [4, 5] }
]

Expected:

✔ status = failure
✔ reason = multi_step_not_supported

---

## TEST 3 — INVALID TOOL

Input:

[
{ "name": "unknown_tool", "args": [1, 2] }
]

Expected:

✔ status = failure

---

## TEST 5 — DETERMINISM

Run same input 3 times.

Expected:

✔ identical outputs

---

# 🔒 SUCCESS CRITERIA

Execution layer is valid ONLY if:

* all tests pass
* outputs match contract
* determinism holds

---

## 🚨 ORCHESTRATOR EXECUTION RULE

All execution paths — including those initiated by the orchestrator — MUST pass through system_entry.

This applies to:
- orchestrator step execution (fallback path — currently enforced)
- agent tool execution (USE_TOOL: path — currently a KNOWN GAP, integration PENDING)

Tests verifying orchestrator behavior MUST confirm:
- system_entry is called for fallback steps
- agent tool execution produces correct output structure
- no execution bypasses validation layer

---

END OF FILE
