# AI LAB — STEP SCHEMA CONTRACT V2
## DRAFT FOR HEAD DEV / SA REVIEW

---

## Document Control

Field               Value
Project             AI Lab / MutesHand
Date                2026-06-30
Status              DRAFT V2 — Pending Head Dev / SA Review
Prepared by         Sub-Dev (Windsurf)
Supersedes          STEP_SCHEMA_CONTRACT_V1.txt
Related docs        PLAN_STEP_CONTRACT_V2.txt (plan step structure)
                    STEP_IO_CONTRACT_V2.txt (step I/O semantics)
                    STEP_RESOLUTION_CONTRACT_V2.txt (step resolution)
                    PLANNING_COMPILER_CONTRACT_V2.txt (planning compiler)
                    DEPENDENCY_MODEL_CONTRACT_V2.txt (dependency semantics)
                    AGENT_CAPABILITY_ROUTING_CONTRACT_V2.txt (capability routing)
                    GOVERNANCE_CONTRACT_V3.txt (runtime decision authority)
                    TRACE_LOGGING_CONTRACT_V2.txt (trace/logging semantics)

---

## SCOPE

Defines the REQUIRED structure of a STEP within the orchestrator system.

A STEP is the smallest unit of execution that:

- triggers one system_entry call
- is evaluated by governance
- produces a deterministic outcome

STEP_SCHEMA applies ONLY to execution-ready steps.
Planner output MUST NOT be validated against this schema.

This contract distinguishes:
- plan / candidate step (pre-runtime, editable, non-executable)
- runtime / execution step (validated, governed, executable)

---

## CORE PRINCIPLE

STEP = atomic, structured, evaluable unit of work

A STEP must contain enough information to:

- execute deterministically
- evaluate success correctly
- apply governance rules

---

## REQUIRED FIELDS

Every STEP MUST include:

{
  "id": "<unique_step_id>",
  "type": "<step_type>",
  "purpose": "<human-readable goal>",
  "tool_call": "<tool_name arg1 arg2>",
  "expected_outcome": "<what success looks like>",
  "risk": "<LOW | MEDIUM | HIGH>",
  "importance": "<LOW | MEDIUM | HIGH>",
  "resource_targets": ["<resource_identifier>"]
}

---

## FIELD DEFINITIONS

### id

- unique within project
- string
- must not change after creation

---

### type

Must be one of:

- ANALYZE
- RESEARCH
- PLAN
- PROPOSE
- EXECUTE_API
- EXECUTE_LOCAL
- EXECUTE_FILE
- EXECUTE_INSTALL
- EXECUTE_SYSTEM_SETTINGS_SERVICES
- EXECUTE_ENVIRONMENT
- VALIDATE
- GENERATE
- BUILD

---

### purpose

- human-readable description of intent
- used for:
  - debugging
  - governance reasoning
  - UI display

Example:
"Find duplicate files in Downloads folder"

---

### tool_call

STRICT format (matches INTERFACE_CONTRACT_V2):

<tool_name> <arg1> <arg2>

Examples:
square_number 5
web_search "latest ai news"

---

### expected_outcome

Defines success criteria.

Examples:
- "List of duplicate files returned"
- "File successfully deleted"
- "API returned valid JSON response"

Used by governance to determine:
purpose_met = true/false

expected_outcome is advisory semantic scaffolding.
It does NOT own execution truth.
execution_result remains the sole execution truth authority.

---

### risk

Determines execution caution level.

Values:

- LOW → safe, reversible, no cost
- MEDIUM → moderate impact or scope
- HIGH → destructive, costly, or system-level

Risk classification is advisory.
Formal risk model governance is a deferred gap.
Risk values support governance decisions but do not override them.

---

### importance

Determines workflow impact if step fails.

Values:

- LOW → optional step
- MEDIUM → useful but not critical
- HIGH → critical to workflow success

Importance classification is advisory.
It supports escalation and approval decisions per GOVERNANCE_CONTRACT_V3.

---

### resource_targets

- list of affected resources
- used for conflict detection
- REQUIRED for:
  - file operations
  - system changes
  - environment changes
  - installs

- OPTIONAL for:
  - read-only or non-resource actions

Examples:

"C:\\temp\\file.txt"
"env:PATH"
"windows_service:spooler"

---

## OPTIONAL FIELDS

May be included if needed:

{
  "context": {...},
  "retry_strategy": {...},
  "metadata": {...},
  "depends_on": ["<step_id>"]
}

---

### context

- carries data from previous steps
- NOT used by core execution
- used by orchestrator only

---

### retry_strategy

- override default retry behavior
- optional
- governed by EXECUTION_RUNTIME_GOVERNANCE_CONTRACT_V2

---

### metadata

- debugging or tracking information
- optional
- traceable per TRACE_LOGGING_CONTRACT_V2

---

### depends_on

- explicit dependency references
- defines execution order and data availability
- validated per DEPENDENCY_MODEL_CONTRACT_V2

---

## HARD RULES

- Each STEP MUST produce EXACTLY ONE tool_call
- Each STEP MUST map to EXACTLY ONE system_entry execution
- STEP MUST NOT contain multiple actions
- STEP MUST NOT chain tool calls
- STEP MUST NOT be treated as execution truth
- execution_result remains the sole execution truth authority

---

## VALIDATION REQUIREMENTS

Before execution, STEP MUST be validated:

- all required fields present
- tool_call format valid
- risk and importance valid enum values
- type valid
- dependency references valid per DEPENDENCY_MODEL_CONTRACT_V2

Invalid STEP MUST NOT execute.

---

## GOVERNANCE COMPATIBILITY

STEP schema MUST support:

- completion evaluation (purpose + expected_outcome)
- retry decisions (risk + history)
- escalation (importance + patterns)
- approval logic (risk + context)

Governance owns all runtime decisions.
STEP schema provides structured inputs to governance.
STEP schema does NOT override governance.

---

## EXAMPLE STEP

{
  "id": "step_001",
  "type": "EXECUTE_FILE",
  "purpose": "Delete duplicate file",
  "tool_call": "delete_file \"C:\\temp\\file1.txt\"",
  "expected_outcome": "File no longer exists",
  "risk": "HIGH",
  "importance": "MEDIUM"
}

---

## FINAL PRINCIPLE

A STEP MUST be:

- complete
- unambiguous
- executable
- evaluable
- validated
- governed

A STEP is a structured input to the execution pipeline.
It is NOT execution truth.
execution_result defines execution truth.

---

END OF DOCUMENT
