🚨 WINDSURF EXECUTION — AUTHORITATIVE CONTRACT AUDIT MAINTENANCE

==================================================
SYSTEM AUTHORITY
================

ROOT:
E:\MutesHand\Project Docs\

PRIMARY TARGET(S):
<INSERT_TARGET_DOCS>

GLOBAL AUTHORITY:

* HAND_ARCHITECTURE_V2.txt
* SYSTEM_GOALS_V2

SYSTEM CONTEXT:

* SYSTEM_STATE.txt
* ROADMAP_IMPLEMENTATION.txt
* KNOWN_ISSUES.txt

PRIMARY OUTPUT TARGET:

* CONTRACT_LEVEL_AUDIT.txt

==================================================
PRIMARY OBJECTIVE
=================

Audit the specified target contract(s) against ACTUAL SYSTEM IMPLEMENTATION.

Then MAINTAIN and UPDATE:

CONTRACT_LEVEL_AUDIT.txt

This is NOT append-only.

The audit document is a LIVING AUTHORITATIVE EXECUTION AUDIT.

==================================================
CORE BEHAVIOR
=============

Windsurf MUST:

1. UPDATE existing audit entries when implementation status changes
2. DOWNGRADE statuses when regressions or drift are discovered
3. APPEND new sections for newly discovered issues/contracts
4. UPDATE evidence/code references when implementation changes
5. UPDATE drift analysis when contracts and runtime diverge
6. UPDATE summaries/counts/groupings when classifications change

==================================================
CRITICAL RULE
=============

If a section currently marked:

* COMPLETE
* PASS
* COMPLIANT

is NO LONGER TRUE based on current implementation,

Windsurf MUST:

* change the status
* update the evidence
* explain the regression/drift
* update all affected summaries/counts

This is MANDATORY.

==================================================
PROHIBITED BEHAVIOR
===================

DO NOT:

* preserve outdated PASS states
* preserve outdated COMPLETE states
* keep stale audit conclusions
* protect previous audit assumptions
* assume previous audits remain valid
* treat historical conclusions as authoritative

ONLY contracts are authoritative.

Audit state MUST reflect CURRENT implementation reality.

==================================================
PHASE 1 — CONTRACT EXTRACTION
=============================

From target document(s), extract:

* required behaviors
* prohibited behaviors
* lifecycle rules
* dependency rules
* validation rules
* authority boundaries
* retry semantics
* projection semantics
* orchestration rules
* GUI expectations
* state transition rules
* enforcement requirements

==================================================
PHASE 2 — IMPLEMENTATION VERIFICATION
=====================================

Inspect ACTUAL implementation INCLUDING:

* orchestrator
* runtime execution
* dependency resolution
* scheduler
* retry handling
* projection generation
* GUI/backend interfaces
* workflow execution
* lifecycle propagation
* persistence
* state management
* validation layers
* streaming/event handling

ALL findings MUST be code-verified.

NO assumptions.
NO speculative claims.
NO inferred implementation.

==================================================
PHASE 3 — REQUIREMENT CLASSIFICATION
====================================

For EACH requirement classify:

* ✅ COMPLETE
* ⚠️ PARTIAL
* ❌ NOT IMPLEMENTED
* 🚨 DRIFT
* 🔥 REGRESSION
* ⛔ CONTRADICTION

==================================================
PHASE 4 — AUDIT RECONCILIATION
==============================

Before writing updates:

1. Locate ALL existing related audit entries
2. Compare old conclusions vs current implementation
3. Detect:

   * regressions
   * stale PASS states
   * stale COMPLETE states
   * outdated evidence
   * outdated roadmap assumptions
   * changed architectural behavior

==================================================
PHASE 5 — REQUIRED AUDIT MAINTENANCE
====================================

Windsurf MUST:

UPDATE existing entries when:

* status changed
* implementation changed
* enforcement changed
* drift introduced
* regression introduced
* architecture changed

APPEND new entries ONLY when:

* issue/category does not already exist
* new contract area discovered
* entirely new root-cause identified

==================================================
PHASE 6 — SUMMARY RECONCILIATION
================================

Windsurf MUST update:

## CONSOLIDATED AUDIT SUMMARY (DE-DUPLICATED)

INCLUDING:

* group classifications
* counts
* totals
* priority order
* drift groups
* regression groups
* roadmap assumptions

==================================================
GROUPING RULE
=============

GROUP BY ROOT CAUSE.

NOT:

* file
* contract section
* implementation location

==================================================
STATUS TRANSITION RULES
=======================

Allowed transitions:

COMPLETE → PARTIAL
COMPLETE → DRIFT
COMPLETE → REGRESSION
PARTIAL → COMPLETE
PARTIAL → DRIFT
DRIFT → COMPLETE

etc.

Status changes are REQUIRED when implementation reality changes.

==================================================
DRIFT RULE
==========

Use DRIFT when:

* implementation diverges from contract
* projections diverge from authority
* roadmap assumptions no longer true
* behavior changed without contract alignment
* enforcement became inconsistent

==================================================
REGRESSION RULE
===============

Use REGRESSION when:

* previously compliant behavior broke
* enforcement weakened
* guarantees were lost
* implementation reverted
* validation/protection disappeared

==================================================
OUTPUT RULES
============

Maintain EXISTING formatting style already present in:

CONTRACT_LEVEL_AUDIT.txt

INCLUDING:

* section formatting
* grouping style
* summaries
* totals
* headers
* roadmap impact sections

==================================================
FINAL PRINCIPLE
===============

Contracts are authoritative.

Audit state reflects CURRENT implementation reality.

Historical audit conclusions are NOT authoritative if implementation changed.
