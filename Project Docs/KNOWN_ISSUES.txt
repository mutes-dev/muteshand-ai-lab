# AI LAB — KNOWN ISSUES (NON-P0)

---

## PURPOSE

This document tracks **NON-P0 system limitations**.

These issues:

* DO NOT break architecture
* DO NOT block execution pipeline
* DO NOT cause instability

They are:

* expected at this phase
* safe to defer
* planned for future improvement

---

## 🚫 WHAT DOES NOT BELONG HERE

This document MUST NOT contain:

* P0 issues
* architectural violations
* temporary deviations
* revoked or fixed issues

Those belong in:

* CONTEXT.txt (system state)
* DECISION_LOG.txt (tracked decisions)

---

# ⚠️ ACTIVE NON-P0 ISSUES

---

## 1. PARSER QUOTED STRING LIMITATION

Parser extracts numeric arguments and quoted strings.

### STATUS

✔ By design
⚠ Known limitation

### EXPLANATION

Quoted strings do not support escaping or nesting.
Complex string handling may have edge cases.

### ACTION

Future phases may enhance quoted string support while maintaining determinism.

---

## 2. PLANNER RULE-BASED LIMITATION

Planner uses simple keyword matching:

* substring detection
* " then " splitting
* TOOL_RULES lookup

### STATUS

✔ By design
⚠ Known limitation

### EXPLANATION

Deterministic behavior is enforced.
No NLP, no inference, no semantic understanding.

### ACTION

Planner Hardening phase will expand coverage while maintaining determinism.

---

## 3. LLM NON-DETERMINISM (CONTROLLED)

LLM output may vary between executions when enabled.

### STATUS

✔ Controlled via validation layer
⚠ Known limitation

### EXPLANATION

LLM is non-deterministic by nature.

System mitigates this via:

- strict prompt constraints
- JSON-only output requirement
- validation layer enforcement
- fail-safe fallback (passthrough)

### ACTION

Future phases may introduce:

- response caching
- retry strategies
- stricter prompt templates

---

## 4. DOCUMENTATION DRIFT (RESOLVED)

Documentation realignment COMPLETE.

### STATUS

✅ RESOLVED

### EXPLANATION

- All documents aligned to code behavior
- SYSTEM_CONTRACTS.txt updated
- CONTEXT.txt rewritten
- HARNESS_SPEC.txt updated
- DECISION_LOG.txt updated
- Strict output contract documented
- Field standardization documented (name not tool)

### ACTION

Complete. All documentation reflects actual code behavior.

---

## 5. INPUT NORMALIZATION

Input normalization is functional but basic.

### STATUS

✔ Functional

### EXPLANATION

- Noise word removal limited
- Casing normalization applied

### ACTION

Future enhancement: expand noise word dictionary.

---

## 6. HARNESS CONTRACT VALIDATION (RESOLVED)

Harness now enforces strict contract validation.

### STATUS

✅ RESOLVED

### EXPLANATION

- STRICT schema enforcement implemented (hard gate)
- NO trace dependency in validation
- NO tool field validation (uses name field)
- Contract structure validation: status + result/reason ONLY
- Tool output normalization verified

### ACTION

Complete. Harness is PRIMARY VALIDATION TOOL.

---

## 7. FLOAT SUPPORT CONTRACT GAP

Float support exists in parser/execution but not in contracts.

### STATUS

⚠ Known limitation

### EXPLANATION

- Parser extracts floats
- Execution processes floats
- Registry supports float type
- Contracts do not explicitly define float behavior

### ACTION

Update SYSTEM_CONTRACTS.txt to formalize float support:
- Define numeric type as int | float
- Document float validation rules
- Document float execution rules

---

## 8. RESOLVER PHRASE STRIPPING (RESOLVED)

Phrase stripping moved from Resolver to Planner.

### STATUS

✅ RESOLVED

### EXPLANATION

- Planner performs phrase matching
- Planner generates `clean_input`
- Resolver consumes `clean_input` from step
- Resolver has NO TOOL_PHRASES dictionary
- Single source of truth in planner layer

### ACTION

Complete. Decision logged in DECISION_LOG.txt.

---

## 9. EXECUTION CONSISTENCY VARIANCE

Historical fixes have created execution path variations.

### STATUS

⚠ Known limitation

### EXPLANATION

- Multiple hardening passes on file tools
- Web search underwent V2 rewrite
- Validation rules expanded (empty string rejection)
- Some tools may have inconsistent behavior patterns

### ACTION

Standardization required:
- Audit all tool implementations
- Enforce consistent patterns
- Document tool-specific behaviors

---

## 10. AGENT TOOL EXECUTION BYPASS

Agent tool execution bypass issue resolved.

### STATUS

✅ RESOLVED

### EXPLANATION

- Both USE_TOOL: input path (agent_executor.py:77) and LLM-generated USE_TOOL: path
  (agent_executor.py:234) now route through system_entry directly.
- The temporary deviation (direct tool_executor routing) has been removed.

### ACTION

Complete. Decision logged in DECISION_LOG.txt.

---

## 11. ORCHESTRATOR NOT COVERED BY HARNESS

The core harness does not test the orchestrator layer.

### STATUS

⚠ Known gap — PENDING

### EXPLANATION

- system/tests/test_workflow.py exists but is a standalone script, not harness-integrated
- Orchestrator behavior (workflow execution, retry, trace, persistence) is not harness-verified
- Core pipeline harness is unaffected

### ACTION

Create orchestrator harness integration:
- Test workflow execution end-to-end
- Test retry behavior
- Test persistence
- Test agent routing vs system_entry fallback

---

# 🔒 IMPORTANT RULE

A system that fails correctly:

→ is HEALTHY

A system that succeeds incorrectly:

→ is BROKEN

---

---

## 🔴 CRITICAL

### 12. SYSTEM CANNOT FULLY VALIDATE LOGICAL CORRECTNESS

System has basic intent validation (limited semantic checks).

### STATUS

⚠ Partially addressed — full Validator Layer still required

### EXPLANATION

- A basic intent validator exists at the orchestrator level (intent_validator.py)
- It performs numeric consistency checks (direct match + single-number contradiction detection)
- Correct argument types do not guarantee a semantically correct result
- Full tool selection correctness and output alignment are NOT validated

### ACTION

Expand Validator Layer to cover: tool selection correctness, tool necessity, full output alignment.

---

### 13. SYSTEM CANNOT VALIDATE CORRECT TOOL SELECTION

LLM selects tools — selection may be wrong.

### STATUS

⚠ Accepted limitation — Validator Layer required

### EXPLANATION

- LLM may select `add_numbers` for a subtraction request
- Structural validation does not catch this
- Intent validator (numeric consistency) partially mitigates but does not fully solve

### ACTION

Validator Layer must evaluate whether selected tool matches user intent.

---

### 14. SYSTEM CANNOT ENFORCE WHEN NO TOOL SHOULD BE USED

LLM may trigger tool calls for non-tool queries.

### STATUS

⚠ Accepted limitation — Validator Layer required

### EXPLANATION

- Natural language questions (definitions, explanations) may incorrectly trigger USE_TOOL:
- No mechanism exists to determine if tool use was appropriate
- LLM format guard only validates format — not appropriateness

### ACTION

Validator Layer must classify tool-required vs no-tool-required inputs.

---

## 🟠 IMPORTANT

### 15. MULTI-TOOL LLM OUTPUTS NOT REJECTED

LLM may output multiple USE_TOOL: calls. Only the first is used.

### STATUS

⚠ Known gap — PENDING

### EXPLANATION

- `llm_output.split("USE_TOOL:", 1)` extracts only the first tool call
- Additional tool calls are silently discarded
- No warning or retry is triggered

### ACTION

Add detection: if LLM output contains more than one USE_TOOL: prefix, reject and retry.

---

### 16. FORMATTER MAY PRODUCE INCORRECT OUTPUTS

Response formatter uses LLM — output is not trusted.

### STATUS

⚠ Known limitation — by design

### EXPLANATION

- `_format_tool_output` calls `ollama_llm` to convert raw output to human-readable text
- LLM may hallucinate or produce unrelated responses
- Formatter failure falls back to raw output (safe fallback exists)
- Formatted output is never validated for correctness

### ACTION

Formatter is advisory only. Future improvement: add confidence scoring or skip formatter for math results.

---

### 17. MEMORY SYSTEM CAN LEARN INCORRECT NOISE TOKENS

Execution memory learns from failure→success pairs but may learn valid tokens as "noise".

### STATUS

⚠ Known limitation

### EXPLANATION

- If LLM adds a word that happens to differ between attempts, it may be learned as a bad token
- Filters prevent numeric, quoted, and path tokens — but word tokens are still at risk
- Example: "hello" was learned as a bad token for `subtract_numbers`

### ACTION

Future improvement: require minimum usage_count threshold before applying a learned token.

---

## 🟡 MINOR

### 18. EXTERNAL TOOLS MAY PRODUCE RUNTIME WARNINGS

Tools making external HTTP requests (web_search) may produce SSL or connection warnings.

### STATUS

⚠ Minor — cosmetic only

### EXPLANATION

- DuckDuckGo HTML scraping may trigger HTTPS redirect warnings
- Warnings appear in stdout but do not affect execution result

### ACTION

Suppress warnings or add explicit HTTPS handling in web_search tool.

---

END OF FILE
