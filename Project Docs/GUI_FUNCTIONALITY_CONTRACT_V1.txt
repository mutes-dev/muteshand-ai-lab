# AI LAB — GUI FUNCTIONALITY CONTRACT V1

## SCOPE

Defines ALL GUI functionality, actions, and interaction rules.

GUI mutation legality boundaries are governed by:
MUTATION_LEGALITY_AND_RUNTIME_ISOLATION_CONTRACT_V1

GUI action sequencing is governed by:
ORCHESTRATION_AND_EXECUTION_SEQUENCE_CONTRACT_V1

GUI projection semantics are governed by:
CANONICAL_PROJECTION_MODEL_V1

Workflow Studio operational mutation semantics are governed by:
WORKFLOW_STUDIO_AND_OPERATIONAL_MUTATION_CONTRACT_V1

Workflow cancellation and terminalization semantics are governed by:
WORKFLOW_CANCELLATION_AND_TERMINALIZATION_CONTRACT_V1

---

## CORE PRINCIPLE

GUI = interaction layer ONLY

GUI MUST:

* reflect orchestrator state
* send user intent
* include required execution context

GUI MUST NOT:

* execute tools
* make decisions
* modify execution logic
* bypass mutation legality boundaries
* assume synchronization completion authority
* own retention classification
* own archival semantics

---

# 1. INTERACTION MODES

DEFAULT MODE — CHAT

* user enters requests
* system responds conversationally
* system MAY suggest creating a project

PROJECT MODE

* activated when task becomes structured
* displays workflow (steps, states, outputs)
* allows user control actions

DEBUG MODE (OPTIONAL)

* shows trace logs
* shows detailed system signals
* does NOT affect execution

---

# 2. CORE GUI COMPONENTS

CHAT PANEL

* accepts user input
* displays system responses

PROJECT LIST

* shows all active projects
* supports switching between projects

WORKFLOW VIEWER

* displays project state
* displays steps and states

STEP DETAIL PANEL

* shows purpose, result, output, retry status

EXECUTION OUTPUT PANEL

* shows tool outputs and logs

CONTROL PANEL (CONTEXT-SCOPED)

* shows valid actions for current workflow

PLAN VIEW (TOGGLE)

* ON → editable plan
* OFF → hidden

Plan View displays PLAN STEP (not execution STEP).

NOTIFICATIONS

* approvals
* failures
* completion

---

## CONTINUITY RULES

GUI MUST preserve projection continuity across:

* reload
* reconnect
* pause/resume
* streaming interruption

GUI MUST refresh from authoritative projection sources when continuity becomes stale or invalid.

---

# 3. CONTROL ACTIONS

ALL control actions MUST include context.

REQUIRED CONTEXT:

* workflow_id
* step_id (when applicable)

ACTIONS:

START PROJECT → workflow_id
PAUSE PROJECT → workflow_id
RESUME PROJECT → workflow_id
STOP PROJECT → workflow_id

APPROVE STEP → workflow_id + step_id
REJECT STEP → workflow_id + step_id
RETRY STEP → workflow_id + step_id

EDIT PLAN → workflow_id

RULE:
GUI MUST NOT send actions without required context.

---

## LIFECYCLE ACTION MODEL

GUI actions REQUEST lifecycle transitions.

Lifecycle transitions are validated and committed by Lifecycle Authority.

GUI actions MUST be validated against orchestration sequencing windows per:
ORCHESTRATION_AND_EXECUTION_SEQUENCE_CONTRACT_V1

GUI MUST obey mutation legality boundaries per:
MUTATION_LEGALITY_AND_RUNTIME_ISOLATION_CONTRACT_V1

GUI MUST NOT:

* directly mutate lifecycle state
* assume transition success before authority confirmation
* bypass mutation legality windows
* assume synchronization completion authority

---

# 4. CONTEXT RULES

GUI MUST:

* track active workflow_id
* attach workflow_id to ALL actions
* attach step_id when required

GUI MUST NOT:

* assume global control
* send context-less requests

---

# 5. VISIBILITY RULES

* simple tasks → chat only
* complex tasks → project view
* debug → optional

---

# 6. PLAN INTERACTION

User MAY:

* view plan
* edit plan
* reorder steps

Edits MUST:

* trigger validation
* respect dependencies
* not bypass contracts

---

## ACTIVE EXECUTION EDIT BEHAVIOR

During ACTIVE execution:

* edit requests MAY be rejected
* edit requests MAY be deferred
* execution-affecting edits MUST NOT apply immediately

Mutation legality during ACTIVE execution is governed by:
MUTATION_LEGALITY_AND_RUNTIME_ISOLATION_CONTRACT_V1

Frontend MUST NOT assume mutation success before orchestrator confirmation.

Frontend MUST NOT assume synchronization completion authority.

Frontend MUST render:

* rejection state
  OR
* deferred mutation state

based on authoritative runtime response.

---

## CANONICAL PLAN PROJECTIONS

Plan View renders canonical plan projections.

Plan edits occur through:

* edit requests
* orchestrator validation
* projection regeneration
* projection re-render

GUI MUST NOT directly mutate canonical plans.

---

# 7. MULTI-PROJECT SUPPORT

GUI MUST:

* support multiple projects
* allow switching
* isolate control per project

---

# 8. EXPLICIT WORKFLOW ATTACHMENT AND OBSERVABILITY BOUNDARIES

## EXPLICIT WORKFLOW ATTACHMENT SEMANTICS

Frontend workflow attachment is:

* explicit
* operator-initiated
* context-bound

Frontend tracks:

* workflows it starts
* workflows it restores
* workflows explicitly selected/attached

This is:
intentional architecture.

NOT:
a defect.

---

## FOCUSED WORKFLOW INTERACTION BOUNDARIES

Focused workflow screens:

* are explicit attachment surfaces
* are workflow-scoped
* are interaction-oriented

Focused workflow screens are NOT:

* global runtime discovery dashboards
* auto-discovery surfaces

Focused workflow UI does NOT automatically discover arbitrary runtime workflows.

---

## PLANNING vs REPLAN-REQUIRED UI SEMANTICS

GUI MUST distinguish:

* live planning (QUEUED_LIVE_PLANNING)
* replan-required planning request (QUEUED_REPLAN_REQUIRED)
* recoverable runtime execution

For QUEUED_REPLAN_REQUIRED:

* UI must NOT show as live/restorable runtime
* UI may show Resume Planning / Replan action
* UI must NOT require operator to retype persisted original prompt
* UI must NOT poll stale bg_id indefinitely
* UI must consume backend-authored planning actionability

For QUEUED_LIVE_PLANNING:

* UI may show planning in progress
* UI may allow stream polling for workflow_id resolution
* UI may show pending/progress indicators

Frontend MUST NOT infer:

* live planning from persistence alone
* actionability from Task Hub visibility alone
* recoverability from stale bg_id

Replan UI semantics are governed by:
PLANNING_RECOVERY_AND_REPLAN_CONTRACT_V1.txt

---

## DASHBOARD VS INTERACTION LAYER DISTINCTION

**Dashboard / Observability Layer:**

* passive read-only visibility
* subscribes to runtime feeds
* groups, filters, aggregates
* non-authoritative

**Focused Workflow Interaction Layer:**

* explicit workflow attachment required
* scoped interaction controls
* mutation requests
* execution inspection

These layers are:

* architecturally separate
* MUST NOT be conflated
* MUST NOT substitute for one another

---

## FUTURE OBSERVABILITY LAYER DIRECTION

Future GUI MAY include:

* rich dashboard views
* project/agent grouping
* orchestration topology visualization
* websocket-driven realtime observability

Future GUI MUST maintain:

* thin frontend authority
* explicit workflow attachment for interaction
* non-authoritative observability consumption
* separation between visibility and authority

---

# 9. FUTURE ADAPTIVE WORKFLOW INTERACTION DIRECTION

Future-state direction for adaptive GUI interaction is preserved per:
ADAPTIVE_RUNTIME_ORCHESTRATION_CONTRACT_V1
HUMAN_IN_THE_LOOP_WORKFLOW_RECONCILIATION_V1

Future GUI MAY support:

* adaptive workflow interaction controls
* reconciliation trigger controls
* checkpoint/review semantics
* execution review window controls
* operator-assisted correction interaction

Future adaptive GUI MUST preserve:

* thin frontend authority
* explicit workflow attachment for interaction
* non-authoritative observability consumption
* separation between visibility and authority

Current GUI architecture remains deterministic execution oriented.

---

# 10. OPERATIONAL MUTATION AND TERMINAL SEMANTICS

## RECOVERABLE FAILED SEMANTICS

GUI MUST recognize:

* FAILED is a recoverable terminal state
* FAILED workflows MAY support retry controls
* FAILED workflows MAY support reconciliation controls

GUI MUST NOT:

* treat FAILED as immutable terminal
* disable retry for FAILED workflows arbitrarily
* synthesize retry legality locally

## TASK HUB CONTRACT

Task Hub represents recoverable / actionable work.

GUI MUST include in Task Hub:

* ACTIVE workflows
* PAUSED workflows
* PENDING_RECOVERY workflows
* recoverable FAILED workflows

GUI MUST NOT include in Task Hub:

* COMPLETED workflows
* CANCELLED workflows
* archived workflows
* historical workflows

Task Hub is NOT historical storage.

## HISTORY CONTRACT

History represents previously existing workflows.

History is observability-oriented and inspection-oriented.

History is NOT recoverability-oriented.

History MAY include:

* COMPLETED workflows
* CANCELLED workflows
* archived FAILED workflows
* historical workflows

History MUST NOT:

* suggest retry legality
* imply recoverability
* override lifecycle authority

## RETENTION VISIBILITY DIRECTION

GUI MAY eventually support:

* retention controls
* archival controls
* dismiss controls
* delete controls

Retention actions do NOT alter lifecycle truth.

Lifecycle state and retention state are independent.

## RECOVERABILITY VISIBILITY DIRECTION

GUI SHOULD expose recoverable terminal workflows in actionable surfaces.

GUI SHOULD NOT expose immutable terminal workflows in actionable surfaces.

Recoverability visibility is governed by:
LIFECYCLE_AUTHORITY_CONTRACT_V1

## RETRY SURFACE VISIBILITY

GUI SHOULD expose retry controls for:

* FAILED workflows
* recoverable terminal workflows

Retry controls MUST:

* include required context (workflow_id, step_id)
* request retry through orchestrator
* not assume retry success before authority confirmation

## CANCELLATION CONTROLS

GUI MAY expose cancellation controls for:

* ACTIVE workflows
* PAUSED workflows
* QUEUED workflows

Cancellation controls MUST:

* request cancellation through orchestrator
* not synthesize cancellation authority locally
* not bypass lifecycle authority

Cancellation semantics are governed by:
WORKFLOW_CANCELLATION_AND_TERMINALIZATION_CONTRACT_V1

---

## BLOCKED NO-PROGRESS CEILING GUI INTERPRETATION

For workflows BLOCKED due to max_iterations_exceeded/no_progress_ceiling:

* show as BLOCKED / intervention required
* keep actionable/recoverable surfaces visible
* keep in Task Hub unless archived/dismissed
* do not move to History by default
* do not display as terminal FAILED
* retry/resume/cancel controls depend on backend-authored actionability

Frontend must not synthesize FAILED from BLOCKED reason.

---

## FOCUSED WORKFLOW PERSISTENCE

Focused workflow attachment SHOULD persist:

* until explicitly detached
* or another workflow explicitly attached

Task Hub navigation MUST NOT implicitly sever:
foreground workflow interaction context.

GUI MUST distinguish:

* observability context (passive viewing)
* interaction attachment context (active control)

## LIGHTWEIGHT MUTATION BOUNDARIES

GUI edit mode supports:

* purpose
* expected_outcome
* risk
* importance
* operator annotations
* semantic correction hints

GUI edit mode does NOT support:

* dependency rewiring
* topology mutation
* structural orchestration mutation

Operational mutation boundaries are governed by:
WORKFLOW_STUDIO_AND_OPERATIONAL_MUTATION_CONTRACT_V1

## CURRENT GUI OPERATIONAL LIMITATIONS

Current GUI MAY feel:

* rigid in correction support
* restart-heavy in recovery
* limited in live structural mutation

These are:
current maturity limitations,
NOT intended future-state direction.

Future adaptive GUI direction per:
ADAPTIVE_RUNTIME_ORCHESTRATION_CONTRACT_V1
HUMAN_IN_THE_LOOP_WORKFLOW_RECONCILIATION_V1

---

# FINAL PRINCIPLE

GUI reflects truth.
GUI sends intent WITH context.
GUI never defines execution.
GUI obeys mutation legality boundaries.
GUI obeys orchestration sequencing authority.
